/**
 * 오피스 좌표 — 지도와 링크가 같은 값을 봐야 하므로 한 곳에서 관리한다.
 *
 * 출처: Google 비즈니스 프로필「TAG EIGHT合同会社」의 등록 좌표(2026-08-26 확인).
 * 대표 확인으로 이 위치가 정확하다.
 *
 * 정정 이력
 *   35.684447  / 139.742919   麹町一丁目 블록 중심(Nominatim). 약 100m 오차
 *   35.6850827 / 139.7421486  Google 지도 우클릭 지점. 건물에서 약 80m 북쪽
 *   35.6843439 / 139.7420912  ← 현재. 프로필 등록 위치와 일치
 *
 * ⚠ 여기를 고치면 지도 핀만 움직인다. 아래 GMAPS_URL 은 프로필 링크라 따로 관리된다.
 */
export const OFFICE = {
  lat: 35.6843439,
  lng: 139.7420912,
  /** 17 = 건물 단위. 16 은 가로 약 1.2km 라 건물이 점으로만 보인다 */
  zoom: 17,
} as const;

/**
 * Google 지도 링크 — Google 비즈니스 프로필(TAG EIGHT合同会社)의 공유 URL.
 *
 * ⚠ 좌표 기반(`?api=1&query=위도,경도`)을 쓰면 안 된다.
 *   그 형식은 「그 좌표에서 검색」이라 등록된 업체가 아니라 근처 아무 POI 를 잡는다.
 *   이 링크는 업체 자체를 가리키므로 열면 회사명·영업정보가 함께 뜬다.
 *
 * 위 OFFICE 좌표와 같은 지점을 가리킨다 — 지도 핀과 링크가 어긋나지 않는다.
 */
export const GMAPS_URL = 'https://maps.app.goo.gl/R4sR6Q9icqMNuWfJ8';
