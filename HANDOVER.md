# TAG EIGHT 사이트 — 인수인계 (2026-08-24)

## 0. 새 대화창에서 가장 먼저 할 일

```
1. 최신 zip 확인:  tag-eight-web_v171.zip
2. 로컬 경로:      ~/Downloads/tag-eight-web   (Mac mini)
3. PRD 읽기:       PRD.md  ← 모든 결정의 근거가 여기 있다
4. 트랜스크립트:    /mnt/transcripts/  (journal.txt 에 카탈로그)
```

🔴 **작업 폴더는 `~/Downloads/tag-eight-web-old` 다.** 여기에만 `.git`(origin/main)이 있다.
   `~/Downloads/tag-eight-web` 은 2026-08-24 에 잘못 만든 사본 — 삭제 대상.
   폴더 이름이 반대라 하루를 날렸다. 정리할 것.

🔴 **`~/Downloads/tsconfig.json` 을 되살리지 말 것.** (`_stray-tsconfig.json.bak` 로 격리)
   Vite 는 tsconfig 를 **상위 폴더까지 거슬러 찾는다.** Downloads 에 놓인 이 파일이
   `astro/tsconfigs/strict` 를 참조하는데 거기엔 node_modules 가 없어
   프로젝트 tsconfig 를 아무리 고쳐도 `Tsconfig not found` 가 계속 났다.

⚠️ `update.sh` 는 Downloads 안에서 **가장 최근 zip**을 집는다.
   실행 첫 줄에 찍히는 `▶ 사용할 파일` 버전이 받은 것과 같은지 반드시 확인한다.
   (2026-08-24, v136을 안 받은 채 v135로 빌드해 「수정이 반영 안 된다」로 헤맴)

**PRD.md 가 단일 진실 소스다.** 「왜 이렇게 했는가」와 「왜 이건 안 하는가」가 전부 기록돼 있다.
같은 논의를 반복하지 않으려면 손대기 전에 해당 섹션을 먼저 읽는다.

---

## 1. 프로젝트 기본

| | |
|---|---|
| 스택 | Astro SSG + Cloudflare Pages + GitHub |
| 언어 | ja(기본) / ko / zh-TW — **58 페이지** |
| 배포 | GitHub `hyun-tag8/tag-eight-web` → `tag-eight-web.pages.dev` (자동 배포) |
| 현재 버전 | **v171** |
| 로컬 확인 | `./update.sh` → `localhost:4321` (Mac mini, Node v24) |

### QA 스크립트 (`/home/claude/`)
```
qa.py          47p × 3뷰포트 — 가로넘침 / JS에러 / alt누락
footercheck.py 푸터 아래 빈 공간
lines2.py      멤버 소개문 줄 수 (2줄 고정 규칙 검증)
```
**변경 후 반드시 3개 다 돌린다.**

### 빌드·패키징 루틴
```bash
cd "/home/claude/site/tag-eight 21" && npm run build
pkill -f "http.server 4321"; cd dist && python3 -m http.server 4321 --bind 127.0.0.1 &
# 검증 후
cd /home/claude && rm -rf pkg && mkdir -p pkg/tag-eight
cd "/home/claude/site/tag-eight 21" && cp -R src public functions /home/claude/pkg/tag-eight/
cp astro.config.mjs tsconfig.json package.json README.md DEPLOY.md PRD.md \
   CONTACT_SETUP.md deploy.sh update.sh /home/claude/pkg/tag-eight/
cd /home/claude/pkg && zip -qr /mnt/user-data/outputs/tag-eight-web_v125.zip tag-eight
```

---

## 2. 🔴 공개 블로커 (우선순위순)

| # | 항목 | 비고 |
|---|---|---|
| 1 | **zh-TW 케이스 10건 HESTER 사인오프** | v137에 초안 투입 완료. **검수 전까지 공개 보류** |
| 2 | **애널리틱스 미설치** | 스크립트 0개. 공개 전 설치 안 하면 베이스라인이 사라진다 |
| 3 | **도메인 tag-8.com 연결** | eNom에서 A레코드만 교체. MX 5개·SPF TXT 절대 미변경 |

**완료됨** — GitHub+Cloudflare 연결(08-20) / 문의폼 GAS+Gmail 경유(08-23, Resend·Formspree 폐기) / Privacy 페이지 / **멤버 5명 사진 본인 확인(08-25 대표 확인 완료)**

## 3. 🟡 진행 예정

| 항목 | 상태 |
|---|---|
| zh-TW 케이스 원고 | **v137에서 10건 투입 완료**(ja 10 / ko 10 / zh-TW 10). HESTER 검수 대기 |
| INSIGHT 페이지 | 빈 페이지 |
| 실물 덱 전체 개정 | 사이트 확정 후 맞춘다 |
| 케이스별 이미지 | `cases.ts` 에 `image` 필드 없음. 현재 도시 히어로 이미지 재사용 |

### ⚠ 덱과 갈린 항목 (개정 시 일괄 반영)
```
MISSION 문구
자본금 550만 (덱 p14 · 명함 · 견적서는 500만)
WHAT WE DO 3영역 명칭:
  戦略設計・現地最適化      → ブランド戦略・現地最適化
  クリエイティブ制作        → クリエイティブ開発・コンテンツ制作
  キャンペーン実行・検証     → プロモーション実行・メディア運用
```

---

## 4. 확정된 디자인 원칙 (건드리지 말 것)

### 4.1 호버 — 큰 제목은 반응하지 않는다
```
큰 제목    이미 그 화면의 주역 → 변화 없음
작은 층    색 + 굵기 + 확대로 살아난다
```
| 위치 | 반응 대상 |
|---|---|
| 홈 WORKS | 분류(`メディア戦略`)에 개인색 + w500 + scale(1.12) / 루트는 scale만 |
| **WORKS 목록** | **행 전체가 검정으로 반전**(아래→위 차오름). 제목·분류·방향·번호가 함께 흰색으로 |
| 홈 CAPABILITIES | 일본어 영역명에 개인색 |
| CAPABILITIES 페이지 | 라벨 scale(1.18) + 항목에 개인색 |

> **홈과 상세는 반응 대상이 달라도 된다.** 홈은 훑는 자리, 상세는 읽는 자리.
> **통일하지 말 것.**

### 4.2 개인색 — 배경에 따라 값이 다르다
```
01 #F37021  DONGHYUN
02 #3F8F63  YONGJUN
03 #2E6FB7  JEEHYUN
04 #C0417A  HESTER
05 #6B7043  SHISEI   (홈 미등장 — 케이스 4장이라)
```
- **흰 배경**: 위 원본색 그대로. `ink` 변형(#B54A15 등)은 **폐기** — 원본보다 어두워 「진해 보인다」
- **PEOPLE `#` 마크**: 같은 색 + **불투명도 42~46%** → 화면상 `#FABD99` 등 파스텔로 보임.
  그 파스텔 값을 텍스트에 쓰면 대비 1.6~2.0:1 로 읽히지 않는다
- **네온(`text-shadow`)은 쓰지 않는다** — 무채색 사이트에서 깔끔하지 못하다

### 4.3 전환
```
color / font-weight :  .22s cubic-bezier(.22, 1, .36, 1)
transform           :  .3s  cubic-bezier(.22, 1, .36, 1)
```
`ease` 는 시작이 굼떠 「딱딱 끊긴다」고 느껴진다.

### 4.4 CJK 자간
| 층 | 값 |
|---|---|
| 일본어 큰 제목 | `+0.005em` |
| 일본어 본문 | `+0.02em` |
| 작은 설명 | `+0.01em` |
| 영문 라벨 | `.24em` (WHY 포함 — `.3em` 은 흩어진다) |
| 숫자·라틴 전용 | **음수 유지** (`.num` -0.04 / `.facts__n` -0.05) |

⚠ `.tm__bio` 만 `+0.008em` — 표준(0.02)을 주면 **2행 규칙이 깨져 3행으로 넘친다**

### 4.4-b WORKS 목록 (2026-08-25 · v146)

```
3열   번호 3rem │ [분류 라벨]      │ 방향 9rem
                 [제목 1행 nowrap]
```

**호버 = 행 전체가 아래에서 위로 검게 차오른다.** (0.38s)

| 왜 이렇게 갔나 | |
|---|---|
| 태그를 **박스로 감싸지 않는다** | 이 페이지는 선(rule)만으로 구성된 에디토리얼 목록이다. 테두리 사각형을 넣으면 컨테이너 언어가 둘이 되고, 11px 짜리 작은 박스는 UI 칩(버튼·폼)으로 읽힌다. v138~v140 에서 1박스·가로2박스·세로2박스를 전부 만들어 붙여본 뒤 폐기 |
| 반전을 **행 단위**로 | 실제 링크는 행 전체다. 작은 칩만 반전시키면 「이 칩을 눌러라」로 읽혀 반응 영역과 클릭 영역이 어긋난다 |
| 분류를 **제목 위**로 | 시선이 번호 → 분류 → 제목 순으로 흐른다. 우측이 비면서 조용해지고, 방향만 오른쪽 열에 정렬돼 스캔이 된다 |
| 검정 반전은 **기존 문법** | CLOSING CTA(.btn)와 같다. 새 장식을 추가한 게 아니다 |
| 분류·방향 **같은 농도**(--ink-55) | 둘 다 제목에 딸린 메타 정보라 층이 같아야 한다. 방향을 --ink-25 로 두면 얼룩으로 보인다 |
| 호버 시 메타 2종을 **순백 + 한 단 굵게** | 검정 위에서 회색(72%/55%)은 죽는다. 흰색으로 올려야 시선이 모인다. 번호만 45% 로 남겨 앵커 역할 |

**분류·방향 크기** — `--step--1`(11.5px) → **`--step-0`(15.7px)**.
11.5px 라벨은 글자가 아니라 얼룩으로 읽힌다. 한 단 올리면서 자간도 같이 내렸다
(분류 `.13em → .1em`, 방향 `.02em`). 글자가 커지면 같은 자간이 더 벌어져 보인다.
방향 열은 9rem 고정 — 3개 언어 전부 넘침 0 확인.

**굵기 표 (호버 전 → 후)**

| | 평상시 | 호버 |
|---|---|---|
| 제목 | 200 | **300** |
| 분류 | 400 | **500** |
| 방향 | 400 | **500** |
| 번호 | — | 흰색 45% |

⚠ 로드된 굵기는 **200 / 300 / 400 / 500 뿐**(Noto Sans JP · Inter). 600·700 을 쓰면 500 으로 떨어져 **아무 변화가 없다.** 더 굵게 가려면 폰트 URL에 700 추가 = CJK 서브셋 한 벌 추가 다운로드.
✅ CJK 글리프는 전각 고정이라 **굵기가 바뀌어도 글자 폭이 안 변한다.** 실측 200→300 에서 폭 변화 0px — 리플로우 없음.

**제목 1행** — 폭이 아니라 글자 크기(`clamp(1.35rem, 2.6vw, 2.25rem)` + `nowrap`)로 맞춘다.
실측 여유(가장 긴 ko 케이스 10번): 1024px **86px** / 1280px 180px / 1920px **80px**
호버 시에는 `padding-left: 1.5rem` 만큼 열이 좁아진다 → ko 1024px **60px** / 1920px **56px**
⚠ 1024·1920 두 끝이 가장 빡빡하다. **ko 원고를 늘리면 여기가 먼저 깨진다.**
3열 시작은 **64rem**. 그 아래에서는 1행이 성립하지 않아 접히게 두고, 검은 판도 적용하지 않는다.

**버전 확인** — 라이브에서 `curl -s .../works/ | grep -o 'data-layout="works-v162"'`
(`grep -c "case-tag"` 같은 느슨한 검사는 구버전도 통과한다. 2026-08-24 에 이걸로 하루 헛돌았다)

### 4.4-c 라벨 크기 — 전 페이지 1값 (2026-08-25 · v145)

**하위 페이지 머리를 홈 섹션 머리(80px)에 맞췄다.** 방향은 「키우기」다.

| 클래스 | 쓰이는 곳 | 크기 |
|---|---|---|
| `.seclabel` | 홈 섹션 머리 · 전 페이지 클로징 CTA | **80px** w500 |
| `.page-head .label` | 하위 페이지 머리 (WORKS / CAPABILITIES / JOURNAL / CONTACT / PEOPLE / 404) | **80px** w500 |
| `.label` (그 외) | 케이스 상세 課題·解決策·本質 / ABOUT VISION·MISSION·VALUES / CONTACT 항목 라벨 / COMPANY 하위 섹션 | 11.52px w400 |

크기는 **`--seclabel-size` 하나**로 홈과 하위 페이지가 함께 움직인다.

⚠ **`.page-head` 안의 `.label` 만 커진다.** 본문에 딸린 라벨까지 키우면 위계가 무너진다.
   ABOUT 은 `page-head--band` 만 갖고 `page-head` 는 없어서 제외된다 — VISION/MISSION/VALUES 가
   같은 리듬이어야 하므로 의도된 제외다.

**라벨 → 제목 간격**을 홈과 같은 `clamp(1.75rem, 4vh, 2.5rem)` 으로 통일했다.
CONTACT 만 라벨 → kicker → h1 3단이라 kicker 에 그 간격을 주고 h1 은 `1rem`.

**현재 전 페이지 확정값** (1440px 실측)

| 층 | 값 |
|---|---|
| 페이지·섹션 머리 라벨 | 80px · w500 · ls .02em · uppercase · --ink |
| 페이지 제목 `.h-lg` | 46.4px · w300 |
| 중제목 `.h-md` | 29.12px · w200 |
| 본문 라벨 `.label` | 11.52px · w400 · ls .24em |
| 리드 `.lead` | 15.68px · w300 |

### 4.4-d `ch` 함정 — CJK 에서 쓰지 말 것 (2026-08-25 · v146)

`ch` 는 **숫자 0 하나의 폭**이다. CJK 전각 글자의 약 **절반**이라,
`72ch` 로 잡으면 실제로는 36자 남짓에서 줄이 끊긴다.

| 고친 곳 | 지정값 | 실제 담긴 폭 | 필요 폭 | 결과 |
|---|---|---|---|---|
| 홈 CLIENTS 주석 `.sector__note` | 72ch | 418px | ja 580 / ko 629 | 2행 → **1행** |
| WORKS ※ 주석 | 46ch | — | — | 2행 → **1행** |

둘 다 `max-width: none` 으로 해결. ja/ko/zh-TW × 1024·1280·1440·1920 전부 1행 확인.

**아직 남아 있는 `ch` 지정** (제목류 — 여러 행이 정상이라 그대로 둔다)
`.h-lg` 24ch · `.h-md` 26ch · `.case-hero__title` 24ch · `.case-nav__title` 24ch
→ **1행으로 세워야 하는 문장에 ch 를 쓰면 반드시 걸린다.** 그때는 `--measure-cjk` 나 px 로.

### 4.4-e 케이스 상세 (2026-08-25 · v147)

목록을 두 단계 올린 뒤 상세가 뒤처져 있었다. 목록의 문법을 그대로 내렸다.

| 문제 | 조치 |
|---|---|
| 히어로 메타가 `09 분류 — 방향` 한 줄 | **목록과 같은 3열** — 번호·분류 왼쪽, 방향 오른쪽 끝, 제목은 아래 |
| 히어로 제목 30px < 목록 제목 36px | **46.4px**(`.h-lg` 와 같은 값). 목적지 페이지가 목록보다 작으면 위계가 뒤집힌다 |
| `.split(1.1fr 1fr)` — 라벨 하나 때문에 화면 왼쪽 절반이 빔 | **`.casebody` 신설**: `10rem` 라벨 열 + 본문. 본문이 왼쪽 축으로 돌아온다 |
| 라벨 11.5px 이라 왼쪽 열이 「빈칸」으로 보임 | `clamp(1.15rem, 2.2vw, 1.7rem)` w500 · `--ink-25`. 페이지 머리(80px) 아래 층 |
| 本質이 26ch 때문에 5행으로 접힘 | `--measure-cjk` + 크기 `clamp(1.4rem, 2.8vw, 2.3rem)` → **3행** |
| case-nav 오른쪽 칸이 우측 정렬 | 둘 다 **왼쪽 축**. 호버 시 **검정 반전**(목록 행 반전과 같은 문법) |

### 4.4-f 케이스 상세 4행 (2026-08-25 · v151 · ja 만)

```
課題  │ ────────────────
設計  │ ────────────────      라벨 8rem + 본문. 세로 4행
展開  │ ────────────────
本質  │ ══════════════       마지막 행만 크게
```

| 왜 | |
|---|---|
| **本質을 검정 밴드로 떼지 않는다** | v148 에서 분리했더니 결론이 스크롤 밖으로 밀려났다. 스크롤해야 나오는 결론은 결론이 아니다 |
| 층은 **크기로만** 만든다 | 본문 15.7px → 本質 최대 32px. 배경색·박스를 쓰지 않는다 |
| 3열로 안 나눈다 | v148 3열은 CJK 열 폭이 좁아 줄바꿈이 어색했다(20자/행). 세로 4행이면 원고를 안 건드려도 된다 |
| 수치는 **展開 행에만** | 課題·設計은 정성, 展開는 실측. 층이 섞이면 어디까지가 사실인지 모른다 |

**데이터** — `Case.design` / `Case.rollout` (둘 다 optional).
**ja 10건만 작성.** ko / zh-TW 는 원고 미작성이라 **자동으로 기존 3블록(課題/解決策/本質)으로 폴백**한다.
→ ko·zh 원고가 나오면 필드만 채우면 레이아웃이 알아서 바뀐다.

**익명 원칙 (2026-08-25 대표 확정)** — 클라이언트·브랜드·지자체·지명 전부 익명.
`中部地域 → この地域` / `熊本 → その地域` / `FunPASS → 現地の観光パス`.

**페이지 높이** 2185px(v146) → 1897(v147) → 1563(v148) → **1621(v149)**.

**버전 확인** — `curl -s .../works/without-words/ | grep -o 'data-layout="case-v151"'`

### 4.4-g 케이스 사진 전면 삭제 (2026-08-25 · v150)

**케이스에 붙어 있던 사진을 전부 뺐다.**

| 어디 | 무엇이었나 | 왜 뺐나 |
|---|---|---|
| 케이스 상세 히어로 | `hero-seoul / tokyo / taipei` 3장 | **10건이 3장을 돌려썼다.** 도착 시장이 같으면 같은 사진 → 「그 케이스의 사진」이 못 되고 장식이 됐다 |
| CAPABILITIES 케이스 카드 | 같은 3장 + 크롭 위치만 변경 | 같은 이유. `object-position` 으로 다르게 보이려 한 것 자체가 근거 없다는 증거 |

**힉스필드 자산 감사 결과 (2026-08-25 · 63장)**

케이스에 쓸 만한 6장 중 **4장이 사용 불가**다.

| ID | 내용 | 판정 |
|---|---|---|
| `b525b617` | TVING 프로그램 로고 + 출연자 얼굴 | 🔴 방송사 IP + 초상권 |
| `923812b2` | tvN 로고 + 방송 화면 | 🔴 동일 |
| `d8dd5f78` | 예능 스틸 + 출연자 얼굴 | 🔴 동일 |
| `f9ef1b53` | YouTube 화면 + 인물 | 🟡 R10 고지 필요 |
| `2c244a8b` | 매장 진열 + #8 사이니지 | 🟢 사용 가능 |
| `c16d354c` | 타이베이 101 + 인물 | 🟢 R10 고지 후 가능 |

⚠️ **AI 로 만들어도 결과물에 방송사 로고·프로그램명·출연자 얼굴이 재현되면 공개 웹에는 못 올린다.**
   케이스 원고에서 明治·第一三共를 익명 처리한 것과 같은 기준. 제안서(비공개 PDF)와 기준이 다르다.
⚠️ **R10(2026-06-01 시행)** — AI 생성 가상인물은 고지 의무. 인물이 나오는 컷은 표기가 붙어야 한다.

**남은 사진 자산은 그대로 유지** — 홈 히어로 영상 3본, ABOUT, TEAM, CEO, 오피스.
케이스에만 사진이 없다. 이건 결함이 아니라 **근거 없는 이미지를 안 쓴다는 선택**이다.

### 4.4-h 케이스 상세 정리 (2026-08-25 · v151)

| 항목 | Before | After |
|---|---|---|
| 방향 표기 | `日本 → 韓国` | **`日本を、韓国の生活者へ。`** — 화살표는 내부 표기다. 클라이언트가 읽는 자리에는 문장 |
| 분류·방향 밸런스 | 분류만 굵고 진함 | 둘 다 `--ink-55` 같은 층. `／` 로 구분 |
| 제목 ↔ 課題 여백 | 96px | **40px** |
| 라벨 색 | `--ink-25`(34%) | **`--ink-55`(72%)** — 34%는 배경으로 가라앉는다 |
| 본문 폭 | `40em` | **`58em`** — 40자에서 끊겨 3행이 되던 게 1~2행으로 |
| 행간 | 2.05 | 1.9 |
| 本質 | 32px w200 | **15.7px w500** — 크기는 다른 행과 같게, **굵기·농도로만** 층을 만든다 |

**줄 수 실측(ja 10건)** — 課題만 2행, 나머지는 대부분 **1행**.
페이지 높이 1540 → **1231~1321px**.

**방향 문장은 `i18n.works.routeLines`** 에 3방향 × 3언어. `routeKey('日本 → 韓国') → 'jp-kr'`.
목록(WORKS)은 화살표 표기 그대로 — 9rem 열에 문장은 안 들어간다.

⚠️ **죽은 CSS `.casebody` 계열 삭제.** `.essence` 가 두 번 정의돼 뒤쪽(32px)이 이겨서
   本質 크기가 안 줄어드는 버그가 있었다. v149 에서 `.caserow` 로 교체할 때 남은 잔재.

### 4.4-i 용어 — 生活者 / 소비자 / 消費者 (2026-08-25 · v152)

**언어마다 다른 단어를 쓴다. 직역하지 않는다.**

| 언어 | 단어 | 근거 |
|---|---|---|
| ja | **生活者** | 사이트·덱 전체가 이미 통일. WHAT WE DO 「市場の文化と生活者の感情」, CAPABILITIES 「市場・生活者分析」, INSIGHT 설명문, 케이스 02 課題. ja 본문에 `消費者` 는 **0회** |
| ko | **소비자** | 「생활자」는 한국어 일상어가 아니라 번역투 |
| zh-TW | **消費者** | zh-TW 는 이미 「市場・消費者分析」「消費者洞察」로 통일 |

**왜 ja 만 生活者인가** — 일본 광고업계에서 「消費者」는 사람을 구매 단위로 보는 말이고,
「生活者」는 사는 사람 전체로 본다. TAG EIGHT 의 L2 「選ばれる理由を設計する」는
구매 이전의 이유를 다루므로 生活者 쪽이 포지셔닝과 맞는다.

⚠️ **v151 에서 zh-TW routeLines 에 「生活者」를 넣은 것은 오류였다.** v152 에서 「消費者」로 정정.
⚠️ ko CAPABILITIES 항목에 「시장·생활자 분석」이 남아 있던 것도 「시장·소비자 분석」으로 통일.

### 4.4-j WORKS 밴드 이미지 (2026-08-25 · v154)

CAPABILITIES 와 같은 문법 — **사진 → 라벨 → 제목 → 리드**.
`.capband capband--top` + `.page-head--band` 재사용. 새 CSS 없음.

| 파일 | 규격 |
|---|---|
| `works-band.webp` | 2200×1072 (2.05:1) · 77KB |
| `works-band-sm.webp` | 1100×536 · 31KB |

**힉스필드 생성** — nano_banana_pro / 21:9 / 4K / 2안 중 채택. 4 크레딧.
원본 6336×2688 → 2.05:1 중앙 크롭 → cap-band 와 같은 규격.

**컨셉** — 도심 이벤트 집행 현장. 팝업 부스 + 대형 사이니지 + 관중이 스마트폰으로 촬영.

**★ 사이니지 브랜딩은 AI 가 아니라 후반 합성이다**

| 위치 | 내용 |
|---|---|
| 정면 파사드 대형 스크린 | `TAG EIGHT WORKS` |
| 부스 우측 패널 | `#8` (실제 로고 SVG `src/assets/logo-mark.svg`) |
| 우측 세로 사인 | `KOREA / JAPAN / TAIWAN` |

**방법** — AI 프롬프트는 「모든 사이니지를 백지로」 생성. 그 위에 Python(PIL)으로
원근 변환(4점 투영) 합성. 로고는 cairosvg 로 래스터, 서체는 Inter.
불투명도 0.60~0.72 + 가우시안 블러 0.8~1.2 로 발광면에 얹은 느낌을 만든다.
재현 스크립트: 세션 로그 `/tmp/comp.py` (좌표는 원본 6336×2688 기준).

⚠️ **AI 에게 글자를 그리게 하지 마라.** 깨진 유사 문자가 나온다. 백지 생성 + 후반 합성이 정답.
⚠️ 생성 원본이 주황빛을 띠어 **전체 그레이스케일 변환** 후 사용. 사이트는 무채색이다.
⚠️ `.capband` 표시 비율은 **21/6 (3.5:1)**. 2.05:1 로 자르면 상단 문자가 잘린다.
   현재 파일 2520×720. 위쪽 180px 오프셋으로 잘라 파사드 문자를 살렸다.

🔴 **반려 2회. 기록해둔다 — 같은 실수를 반복하지 않기 위해.**

| 버전 | 컨셉 | 반려 사유 |
|---|---|---|
| v154 | 테이블 위 백지 인쇄물(정물) | 「생동감이 없다. 광고회사인지 모르겠다」 |
| v155 | 촬영 현장(시네마 카메라·조명·붐) | 「영상 제작회사는 아니다」 |

**교훈 2개**
1. **인물 금지 + 문자 금지를 동시에 걸면 문구점 사진이 된다.** 케이스 사진의 IP 기준을
   밴드에까지 적용한 게 v154 의 원인. 밴드는 실적 근거가 아니라 회사의 장면이다.
2. **카메라를 찍으면 제작사가 된다.** TAG EIGHT 는 기획·매체 설계·3국 실행·집행이다.
   장비가 아니라 **판단하는 장면**을 찍어야 한다.

**핵심 장치 — 매체를 형태로 읽힌다**
벽의 프레임 비율이 매체를 말한다. 세로 긴 것=숏폼, 와이드=옥외 사이니지, 16:9=영상.
모니터도 세로·가로 프레임. **글자 하나 없이 「무슨 회사인지」가 전달된다.**

**대안(보관)**
- `v156_대안_이벤트집행.webp` — 도심 팝업 부스 + 대형 사이니지 + 관중이 스마트폰 촬영.
  집행·이벤트 축이 강하고 3국 도시 느낌도 나온다. 밴드를 바꾸고 싶을 때 이걸로 교체.
- `v155_대안_교차로.webp` — 교차로 롱익스포저. WORKS 와 관계가 약해 폐기 판정.

⚠️ **프롬프트에 「글자·숫자·로고 일절 금지」를 명시했다.** 인쇄물이 소재라
   그냥 두면 판독 불가능한 가짜 문자가 생성된다. 재생성 시 이 조건 유지.
⚠️ **얼굴은 모션블러로 판독 불가.** 실루엣·뒷모습만. 초상권 리스크 없음.
⚠️ **R10(2026-06-01 · AI 가상인물 고지 의무)** — 인물이 들어갔으므로 공개 전 고지 방식 확정 필요.
   ABOUT 팀 사진·홈 히어로 영상도 같은 조건이라 **사이트 단위로 한 번에** 처리한다.

### 4.4-k 케이스 하단 (2026-08-25 · v158)

```
이전 │ 다음        ← 좌우 2칸. 호버하면 칸이 검정으로 차고 번호가 1.7배
──────────────
事例一覧へ          ← 중앙 정렬 1행. 호버하면 칸 전체 검정
──────────────
CONTACT            ← 클로징 CTA 신설
```

| 항목 | 내용 |
|---|---|
| **축 정렬** | `.case-nav` 를 `.wrap` 안으로. 이전에는 본문보다 **80px 왼쪽**에서 시작해 어긋났다(조잡함의 주원인) |
| **번호 확대** | `transform: scale(1.7)`. `font-size` 를 쓰면 제목이 밀린다 |
| **기준점** | `transform-origin: left bottom` **양쪽 다.** 오른쪽 칸에 `right bottom` 을 주면 칸 밖으로 자라 잘린다 |
| **호버 배경** | `::before { margin-inline: -100vw }` + `.case-nav { overflow: hidden }` 로 화면 끝까지. 가운데 선 쪽은 넘기지 않는다 |
| **여백** | `padding-block` 을 `2.25~3.25rem` 으로 확대 |
| **`事例一覧へ`** | 중앙 정렬 풀폭 밴드. 호버 시 검정 + 자간 `.16em → .26em` |
| 그 크기 | `--step-0`(15.7px) → **`--step-1`(19.2px)**. 페이지를 닫는 유일한 한 줄인데 본문 크기와 같아 묻혔다. 높이도 `2.25~3.5rem` 으로 |
| **클로징 CTA** | **케이스 상세에만 없었다.** 다른 5개 섹션에는 전부 있었다 |

⚠️ `.case-back` 바로 아래가 검정 CTA 라 호버하면 두 블록이 붙어 보인다.
   `border-bottom-color: rgba(255,255,255,.22)` 로 경계선 한 줄을 남겼다.

### 4.4-l 화살표 — 문자 금지, 아이콘으로 (2026-08-25 · v162)

**`→` `←` 문자 글리프를 전부 `Arrow.astro` SVG 로 교체했다.**

문자 화살표는 폰트마다 굵기·길이가 제각각이고, **CJK 폰트로 폴백되면 굵고 짧게** 나온다.
본문은 w200~300 인데 화살표만 굵어서 싸구려로 보였다.

**`Arrow.astro` 에 variant 2종.** 둘 다 stroke-width **1** — 사이트 괘선과 같다.

```astro
<Arrow variant="ring" dir="left" />   <!-- 원 + 꺾쇠. 케이스 이전·다음 -->
<span class="btn__slot"><Arrow variant="chev" cls="btn__arrow" /></span>  <!-- 버튼 -->
```

| variant | 구성 | 쓰는 곳 | 이유 |
|---|---|---|---|
| **ring** | 1px 원(r=19) + 꺾쇠 | 케이스 이전·다음 | 유일하게 **「눌러지는 것」**으로 읽힌다. 40px 원이 번호(30px)와 짝이 맞는다 |
| **chev** | 꺾쇠만 | 전 페이지 `.btn` | **평상시엔 없다.** 호버하면 `.btn__slot` 의 폭이 0 → .55rem 으로 열리며 나타난다 |
| line | 가로선 + 꺾쇠 | (현재 미사용) | 넓은 자리용으로 남겨둠 |

**버튼에 선을 상주시키지 않는 이유** — 버튼은 이미 사각 테두리다.
안에 가로선을 또 그으면 **선이 두 겹**으로 보인다. 평상시엔 테두리 1개만, 꺾쇠는 호버할 때만.

⚠️ `.btn { gap: 0 }` 으로 바꿨다. 간격은 `.btn__slot` 의 `margin-left` 가 만든다 —
   그래야 평상시에 빈 자리가 남지 않는다.
⚠️ 라벨 확대(`scale(1.12)`) 선택자에서 `.btn__slot` 을 제외해야 한다.
   빼먹으면 슬롯까지 커져 여는 폭이 어긋난다.
**적용 위치 — 사이트 전체. 문자 화살표 0개.**

| 자리 | 처리 |
|---|---|
| 케이스 이전·다음 | ring — 상주 |
| 전 페이지 `.btn` (홈 WORKS·CAPABILITIES / ABOUT / CONTACT / 클로징 CTA) | chev — 호버 시 열림 |
| CAPABILITIES `VIEW MORE` **6곳** | chev — 호버 시 열림 |
| CONTACT 폼 `送信する` (`.cform__submit`) | chev — **상주**. 밑줄 링크라 테두리가 없어 선이 겹치지 않는다 |
| THANKS `홈으로` (`.link--arw`) | chev — 상주 |

⚠️ 테두리가 **있는** 것(.btn)은 호버 시에만, **없는** 것(밑줄 링크·submit)은 상주.
   판단 기준은 「선이 겹치는가」다.

**검토하고 버린 안**
- 선 화살표 단독 — 안전하지만 특징 없음
- 꺾쇠만 1개/2개 — 작아서 존재감 없음
- `»` 2개 — **미디어 플레이어**로 읽힘
- `PREV / NEXT` 라벨 — 번호와 붙어 답답
- 아이콘 없음 — 가장 깨끗하지만 클릭 신호가 없음

| 사양 | 값 |
|---|---|
| stroke-linecap | `square` — 끝을 자른다. round 는 물러 보인다 |
| 색 | `currentColor` — 부모 색을 받아 반전에 자동 대응 |
| 정렬 | ring `translateY(.35em)` / line `translateY(-.08em)` |
| 좌우 반전 | `scaleX(-1)` — 별도 SVG 를 만들지 않는다 |

⚠️ 케이스 이전·다음의 **다음 칸**은 번호가 `scale(1.7)` 로 커지며 화살표를 덮는다.
   커진 폭만큼 `translateX(34px)` 로 밀어낸다. 왼쪽 칸은 번호가 화살표 반대쪽으로 자라 문제없다.
⚠️ 아직 문자 화살표가 남은 곳: `Thanks.astro`, `Contact.astro` 의 submit 버튼(`{f.submit} →`).
   버튼 내부 텍스트라 다음에 정리.

### 4.4-m CAPABILITIES 케이스 카드 행 길이 (2026-08-25 · v164)

사진 열을 뺀 뒤(v150) 본문이 열 폭 **1136px** 를 다 쓰게 됐다.
CJK 한 줄 **78자** — 눈이 다음 줄 첫머리를 놓치는 길이다.

| 요소 | max-width | 실측 폭 | 행수 |
|---|---|---|---|
| `.capcase__title` | `34em` | 653px | 1~2 |
| `.capcase__essence` | `46em` | 530px | 2~3 |

**`em` 을 쓴 이유** — 자기 글자 크기 기준이라 제목(19.2px)과 본문(11.5px)의
**시각적 폭**이 비슷하게 맞는다. `px` 로 잡으면 글자 크기가 바뀔 때 균형이 깨진다.
`ch` 는 CJK 에서 절반만 담으므로 여기서도 쓰지 않는다(4.4-d 참조).

⚠️ 「본문이 잘린다」는 지적이 있었으나 **실측 결과 넘침은 없었다.**
   스크린샷이 브라우저 창보다 좁게 잘린 것. 실제 문제는 넘침이 아니라 **행 길이**였다.

### 4.4-n 문장 단위 줄바꿈 `Sentences.astro` (2026-08-25 · v166 · 사이트 전체)

케이스 상세 본문(課題·設計·展開·本質)을 **문장마다 한 줄**로 나눈다.
폭에 맡기면 「〜まだ限定的だった。単発の」처럼 앞 문장 끝과 다음 문장 머리가
한 줄에 섞여 읽는 리듬이 끊긴다. 홈 리드문·클로징 CTA 가 쓰던 문법을 본문에도 적용.

각 문장은 `display: block` — **폭이 좁으면 그 안에서 다시 접힌다.** 모바일 안전.

🔴 **정규식 한 줄로 자르면 반드시 깨진다. 파서를 쓰는 이유 3가지.**

| 입력 | 정규식 결과 | 원인 |
|---|---|---|
| `最大2.9倍を記録。` | `最大2.` / `9倍を記録。` | 소수점을 문장 끝으로 인식 |
| `〜は何？」` | `〜は何？` / `」` | 닫는 낫표가 홀로 떨어짐 |
| `「あのお菓子は何？」は、置き場所さえあれば〜` | 두 동강 | **인용 안의 물음표**에서 잘림 |

**파서가 처리하는 것**
1. 인용·괄호 **깊이**를 세어 `「」『』（）` 안에서는 자르지 않는다
2. ASCII `.` 는 앞뒤가 숫자면 자르지 않는다(소수점)
3. 종지부 뒤에 오는 닫는 괄호·따옴표는 **앞 문장에 붙인다**

**적용 범위 — 사이트 전체.** 본문성 텍스트를 전수 스캔해 빠짐없이 적용했다.

| 페이지 | 대상 |
|---|---|
| HOME | WORKS 리드 / SECTORS 리드 / WHAT WE DO 리드 |
| ABOUT | CREED 본문 / WHY 리드 |
| CAPABILITIES | 페이지 리드 / 3블록 리드 / 케이스 카드 本質 |
| WORKS | 페이지 리드 |
| 케이스 상세 | 課題·設計·展開·本質 |
| COMPANY | 대표 메시지 / 멤버 bio |
| CONTACT | 페이지 리드 / 자료 요청 본문 |
| INSIGHT · PEOPLE | 페이지 리드 |

**⬜ 의도적 제외 — PRIVACY `legal__p`**
법률 문서는 문단이 정본이다. 문장마다 줄을 나누면 계약 조항처럼 읽히고
개인정보보호법 고지문의 형식 관례에서 벗어난다.

**원고 개행(`\n`)과의 관계** — 원고 개행은 **유지하고, 그 안에서 한 번 더** 문장으로 나눈다.
CAPABILITIES 리드처럼 「〜その「間」に立ち、」 로 의도적으로 끊은 자리는 그대로 살아 있다.

⚠️ `<br>` 과 함께 쓰지 마라. `Sentences` 가 block 을 만들므로 **빈 줄이 생긴다.**
   COMPANY 대표 메시지·CAPABILITIES 리드에서 `<br>` 을 제거했다.
⚠️ **HOME SECTORS 리드에 있던 옛 정규식 분할(`split(/(?<=[。.？?！!])/)`)도 교체했다.**
   위 세 함정을 그대로 갖고 있던 코드다. `src/lib/wbr.ts` 의 분할은 BudouX 전처리용이라 별개.

**검증** — ja/ko/zh-TW × 10 페이지 = **30 페이지 전수 스캔.**
모든 조각이 종지부 또는 닫는 괄호로 끝나고, 빈 조각 0개, 가로 넘침 0,
미적용 다문장 단락은 PRIVACY 뿐.

### 4.4-o INSIGHT 명칭 통일 (2026-08-25 · v167)

헤더 메뉴는 `INSIGHT`, 페이지 라벨만 `JOURNAL` 로 남아 있었다. 3개 언어 전부 `INSIGHT` 로 통일.

⚠️ **내부 파일명은 아직 journal 이다** — `src/sections/Journal.astro`, `i18n.journal.*`.
   URL 은 `/insight` 로 정상. 파일명 개명은 참조가 많아 다음 세션으로 미룸.
   **표시 문자열만 INSIGHT** 라는 것을 기억할 것.

### 4.4-p INSIGHT 발행 구조 (2026-08-25 · v168)

**주 1~2회 발행을 위해 관리화면을 붙였다. 터미널·zip 불필요.**

```
tag-8.com/admin  →  GitHub 로그인  →  글 작성  →  保存
                 →  저장소에 커밋  →  Cloudflare 자동 빌드  →  2분 뒤 공개
```

| 구성 | 파일 |
|---|---|
| 관리화면 | `public/admin/index.html` + `config.yml` (Sveltia CMS, CDN) |
| 로그인 창구 | `functions/api/oauth.ts` — GitHub OAuth 중계. 토큰 저장 안 함 |
| 스키마 | `src/content.config.ts` |
| 원고 | `src/content/insight/YYYY-MM-DD-<postId>-<lang>.md` |
| 목록·기사 | `src/sections/Journal.astro` / `Post.astro` |
| 라우트 | `/insight` · `/insight/<postId>` (+ `/ko` `/zh-tw`) |
| 가이드 | **`docs/INSIGHT_발행가이드.md`** — 대표에게 이 문서를 준다 |

**언어 구조** — 파일 하나 = 기사 하나. 언어는 프런트매터 `lang`.
같은 글의 언어판은 **`postId` 를 같게** 쓴다. 한 언어만 있어도 그 언어만 생성된다.

**대표가 한 번만 해야 할 것 (약 10분)**
1. GitHub OAuth App 생성 — callback `https://tag-eight-web.pages.dev/api/oauth`
2. Cloudflare Pages 환경변수 `GITHUB_CLIENT_ID`(Plaintext) / `GITHUB_CLIENT_SECRET`(**Secret**)
3. 재배포 1회 (환경변수는 새 배포에만 반영 — 문의폼 때와 같은 함정)

⚠️ **v169 시점 기본값은 `.pages.dev`.** 도메인 연결 후 두 곳을 `tag-8.com` 으로 바꾼다 —
   ① `public/admin/config.yml` 의 `base_url` ② GitHub OAuth App 의 callback URL.
   `functions/api/oauth.ts` 는 두 주소를 **모두 허용**하므로 전환 중에도 끊기지 않는다.
⚠️ `/admin` 은 `robots.txt` + `_headers` 양쪽에서 색인 차단 + `no-store`.
⚠️ **필드를 늘릴 때는 `content.config.ts` 와 `admin/config.yml` 을 양쪽 다** 고친다.
   한쪽만 고치면 빌드가 스키마 오류로 죽는다.

**Astro 6 함정 2건 (오늘 둘 다 밟음)**
- 컬렉션 설정은 `src/content/config.ts` 가 아니라 **`src/content.config.ts`**, 그리고 `loader` 필수
- `entry.render()` 폐지 → **`import { render } from 'astro:content'`** 후 `render(entry)`

### 4.4-q CONTACT 재구성 (2026-08-26 · v171)

**3293px(3화면) → 1792px.** 이전 구조는 머리 → 오피스 사진 → 연락처 → 폼 순으로 쌓여
문의하러 온 사람이 스크롤을 세 번 해야 했다.

```
┌──────────────────────┬───────────┐
│ 지도(다크) 위에         │ 폼 카드    │
│ CONTACT / 카피        │ (흰 판)    │
│ Email·Tel·Address     │           │
└──────────────────────┴───────────┘
```

| 항목 | 내용 |
|---|---|
| 지도 | **CARTO Dark + Leaflet.** `src/components/OfficeMap.astro` |
| 오피스 사진 | **삭제.** COMPANY 에 이미 있다 |
| 전화번호 필드 | 추가하지 않음(대표 판단) |
| 동의 체크박스 | **추가.** 개인정보보호법 21조 |

**왜 Google Maps 가 아닌가** — 다크 스타일로 바꾸려면 CSS 필터를 씌워야 하는데
Google Maps 이용약관상 스타일 변형에 제한이 있다. CARTO 는 다크 타일을 정식 제공하고
저작권 표기(`© OpenStreetMap © CARTO`)만 하면 무료다.

🔴 **좌표는 잠정값이다.** `OfficeMap.astro` 의 `LAT/LNG` 는 麹町一丁目 블록 중심
   (Nominatim 조회, 35.684447 / 139.742919). **정확한 건물 위치로 교체할 것** —
   Google 지도에서 해당 건물 우클릭 → 좌표 복사 → 두 상수만 바꾸면 된다.

**동의 체크는 서버에서도 검증한다** — `functions/api/contact.ts` 에서
`consent !== 'agree'` 면 `?e=1` 로 되돌린다. 클라이언트 `required` 만으로는 우회된다.

⚠️ 지도는 **화면에 들어올 때 로드**한다(IntersectionObserver). 스크롤 확대는 꺼둔다 —
   페이지를 내리다 지도가 확대되는 사고를 막는다. 클릭하면 켜진다.
⚠️ `.omap__link`(Google 지도 링크)는 **지도 컨테이너 밖**에 둔다.
   안에 두면 `.ctpanel__in`(z-index:1)에 덮여 안 보인다. 한 번 밟았다.
⚠️ `.cform label { display:block }` 이 뒤에 오므로 동의 라벨은 `display:grid !important`.

### 4.5 클로징 CTA
`src/components/ClosingCta.astro` — 홈 포함 5페이지 공통.
| 페이지 | CTA |
|---|---|
| 홈 · CAPABILITIES · WORKS · INSIGHT · COMPANY | ● |
| ABOUT | — `.why` 가 이미 검정. 대신 **WHY 안에 `TAG EIGHT →` 버튼**(→ /company) |
| CONTACT | — 페이지 자체가 창구 |

> `CONTACT` → `Connect` 개명안은 **폐기**. 라벨·버튼·내비·페이지 제목 네 곳이 어긋난다.

---

## 5. 기술적 함정 (실제로 밟은 것)

| | |
|---|---|
| **CSS 블록 삭제** | 범위를 넘겨 **13,000자 소실**한 적 있음(`.vpanel` `.svc-brief` `.sector` 등 27개 셀렉터). 삭제 시 시작·끝 마커를 모두 검증하고 **삭제 길이를 assert 로 상한 검사**한다 |
| **`transform: scale()`** | 레이아웃을 밀지 않아 **옆 요소를 덮는다.** `.wcard__meta` gap 을 2rem 으로 올려 해결. 배율 바꾸면 gap 재계산 |
| **`T` 컴포넌트** | `\n` 을 처리하지 않는다(BudouX `<wbr>` 전용). 개행은 `split('\n')` + `<br />` |
| **`ch` 단위** | CJK 에서 절반밖에 못 담는다. 폭 제한에 쓰지 말 것 |
| **한국어가 먼저 넘친다** | 제목 크기 상한은 **ko 기준**으로 잡는다. 현재 홈 WORKS 제목 상한 23.2px 은 **임시값** — ko 원고 확정 후 26px 까지 상향 가능 |
| **`font-size` 호버 변경** | 레이아웃이 밀린다. `transform: scale()` 을 쓴다 |
| **full_page 스크린샷** | lazy 이미지·`.rise` 페이드인이 안 잡힌다. 스크롤 후 섹션별로 찍을 것 |

---

## 6. 카피 확정본 (변경 금지)

### 멤버 소개문 — 5명 확정, 3언어 전부 2줄
| | 축 | 서술어 | 직함 |
|---|---|---|---|
| DONGHYUN LEE | 외국인의 위치 | `探す` | Creative Director & CEO |
| YONGJUN PARK | 20년의 감각 | `動く` | Content Producer |
| JEEHYUN YONG | 매체의 움직임 | `考える` | Media & Social Producer |
| HESTER LIN | 양쪽 목소리 | `いる` | Creative Producer |
| SHISEI AOKI | 사실 | `事実から` | Strategy Producer |

### 페이지 헤드
```
CAPABILITIES  文化の違いを、選ばれる理由に変える。
              韓国・日本・台湾。ブランドの想いと、その市場で暮らす人の感覚。その「間」に立ち、
              戦略からクリエイティブ、実行までをつくります。

홈 CAPABILITIES  考える。つくる。届ける。   ← 세 동사가 3블록과 1:1 대응. 순서 바꾸면 같이 바꿀 것
WORKS            課題の立て方を変えると、答えが変わる。
CONTACT          まだ、選ばれる理由がない。 / そこから始めるのが、私たちの仕事です。
```

### 3블록 리드 — 하나로 이어진다
```
見つける → 形にする → つなげる
```

---

## 7. 대화 스타일

- 한국어로 논의, 일본어는 자연스러운 비즈니스 일본어(직역 금지)
- **결론부터.** 짧고 단정적으로
- 피드백은 감각으로 온다(「올드하다」「없어 보인다」「어색하다」) → **기술적 원인을 진단**해서 답한다
- 시안은 **실제로 만들어서 스크린샷으로 비교**한다. 말로만 설명하지 않는다
- 변경 후 QA 3종 → 패키징 → `present_files` 까지가 한 세트
