# TAG EIGHT 홈페이지 리뉴얼 PRD

- 문서 버전: v1.0
- 작성일: 2026-08-06 (Asia/Tokyo)
- 대상 도메인: tag-8.com
- 작성 기준: 실제 빌드 결과 실측 (`npm run build` 2026-08-06)

---

## 1. 왜 만드는가

### 문제

기존 사이트(STUDIO.design 제작)를 브라우저로 직접 파싱한 결과, **원본 HTML에 본문이 한 글자도 없었다.**

```
/about 원본 HTML 11,257자
  "OUR MISSION"       → 없음
  "TAG EIGHT 合同会社"  → 없음
  "李東眩"             → 없음
  JSON-LD             → 없음
  <html lang>         → 값이 비어 있음
```

브라우저로 열면 보이지만, JavaScript를 실행하지 않는 크롤러에게는 빈 페이지였다.

### 왜 이것이 치명적인가

TAG EIGHT의 케이스 02·06·10은 전부 **「검색되는 자리에 미리 답을 놓아둔다」**는 사상이다.
그 방법론을 파는 회사의 사이트가 정작 검색·AI에 읽히지 않는 상태였다.

Google은 JS를 렌더링하지만, GPTBot·ClaudeBot·PerplexityBot·네이버 검색로봇은
대체로 JS를 실행하지 않는다(각사 공식 문서상 렌더링 지원 명시 없음, **추정**).

### 목표

> **AI와 검색엔진이 TAG EIGHT를 설명할 수 있는 상태를 만든다.**

부수 목표 — 일본어 단일 사이트를 3개 언어로 확장, 케이스 자산의 공개, 저널 발행 기반 확보.

---

## 2. 확정된 결정

이 프로젝트에서 여러 번 방향을 되돌렸다. **최종 확정된 것만** 기록한다.

### 2.1 기술

| 항목 | 결정 | 폐기한 대안 |
|---|---|---|
| 프레임워크 | **Astro (정적 생성)** | STUDIO.design, Framer, Next.js |
| 저장소 | GitHub (Private) | — |
| 호스팅 | **Cloudflare Pages** (무료) | — |
| 언어 | ja(기본) / ko / zh-TW | 일본어 단일 |
| 편집 | 파일 기반 → 추후 Keystatic 검토 | — |

**Astro인 이유**: 기본 출력이 정적 HTML이라 본문이 원본에 실린다. 이것이 프로젝트의 유일한 필수 요건.

### 2.2 디자인

| 항목 | 결정 |
|---|---|
| 코퍼레이트 컬러 | **흰색과 검정.** 포인트 컬러 없음 |
| 배경 / 잉크 | `#FFFFFF` / `#0B0B0A` |
| 본문 서체 | Noto Sans JP·KR·TC, weight 200–300 |
| 라벨·숫자 | Inter, 대문자, letter-spacing 0.24em |
| 사진 | 흑백, 중간 계조 살림 |
| **미디어 톤** | **정지 = 흑백 / 호버 = 컬러 (2026-08-14 확정).** 아래 2.2.1 참조 |
| 리듬 | 흰 섹션과 검정 반전 섹션을 번갈아 |
| 섹션 명칭 | **SERVICE → CAPABILITIES** (2026-08-14). 라우트도 `/capabilities` 로 변경, `/service` 는 301 |
| 폐기 표현 | **「Cross-Border / クロスボーダー / 크로스보더 / 跨境」 전면 금지.** 브랜드메시지 확정판 §3 |
| 줄바꿈 | **BudouX 문절 판정 + `word-break: keep-all`.** 어절 중간 절단 금지 |

**폐기한 방향** — 명조(Shippori Mincho) 계열, 글래스모피즘, 오렌지 포인트 컬러, 추상 도형 다이어그램, SNS 아이콘·세계지도 계열.

### 2.2.1 미디어 톤 규칙 (2026-08-14 확정)

> **정지 상태는 흑백. 호버하면 색이 돌아온다.**

컬러를 포인트 컬러로 쓰는 게 아니다. **사진 자체가 원래 가진 색을 잠깐 돌려주는 것**이므로
「코퍼레이트 컬러는 흰색과 검정」과 충돌하지 않는다. 색은 브랜드가 아니라 피사체의 것이다.

**어디에 적용하나** — 컨테이너에 `class="tone"` 하나만 붙인다.

| 적용 | 위치 | 이유 |
|:--:|---|---|
| ● | 홈 CAPABILITIES 카드 (`.svc-brief__fig`) | 케이스·서비스를 가리키는 미디어. 색이 돌아오면 「실제로 있었던 일」이 강해진다 |
| ● | CAPABILITIES 관련 케이스 (`.capcase__fig`) | 〃 |
| ● | WORKS 상세 히어로 (`.case-hero`) | 〃 |
| ✕ | 홈 히어로 3분할 (`.vpanel`) | 회사 자체를 보여주는 미디어. 여기가 흔들리면 사이트 톤이 통째로 바뀐다 |
| ✕ | ABOUT 타일 (`.tile`) | 〃 |
| ✕ | CONTACT 오피스 (`.office`) | 〃 |

**동작 사양**

- 전이 0.5s `cubic-bezier(.16,1,.3,1)`. `--tone-dur` 로 일괄 조정 가능
- `@media (hover: hover) and (pointer: fine)` 안에서만 발동.
  터치 기기는 탭 뒤에 `:hover` 가 남아 카드 한 장만 컬러로 굳는다
- `a:hover .tone` 도 발동 — 카드 어디를 가리켜도 색이 온다
- `a:focus-visible` 포함 — 키보드 이동에서도 같은 피드백
- `prefers-reduced-motion: reduce` 에서는 전이 없음(상태 전환은 유지)
- `transition` 은 단일 속성이라, `transform`·`opacity` 를 쓰던 선택자는
  **`filter` 를 같은 선언에 병기**해야 한다. 나눠 쓰면 나중 것이 앞 것을 통째로 덮는다

**정지 상태 대비 보정** — `filter: grayscale(1) contrast(1.12)`

컬러 마스터를 CSS로 흑백으로 되돌리면 중간 계조가 늘어 기존 흑백 소재보다 물러 보인다.
정지 상태에만 `contrast(1.12)` 를 걸어 원래 톤으로 되돌린다. 호버 시에는 `contrast(1)` 로 해제.

**⚠ 자산 치수는 코드에 하드코딩돼 있다 — 교체 시 반드시 같이 고칠 것**

| 자산 | 실측 (2026-08-14) | 참조하는 코드 |
|---|---|---|
| `hero-{seoul,tokyo,taipei}.webp` | **1200×600** / `-sm` 800×400 | `CaseDetail.astro` `heroImg` 맵 |
| `work-{think,create,activate}-sm.webp` | **700×467** | `Home.astro` `width/height` |
| `work-*.mp4` | **800×532 (3:2)** | — |
| `about-*.webp` | 928×1152 / `-sm` 560×695 | `About.astro` |
| `office.webp` | 1223×816 / `-sm` 800×534 | `Contact.astro` |

`srcset` 의 `w` 서술자가 실제 폭과 다르면 브라우저가 잘못된 후보를 고른다.
`width/height` 가 틀리면 레이아웃 시프트(CLS)가 생긴다. **자산 교체 = 코드 수정**으로 묶어서 처리한다.

**소재 요건** — 이 규칙은 컬러 마스터 1개만 있으면 된다. 흑백판을 따로 두지 않는다.

**소재 현황 (2026-08-14 실측 채도)** — `.tone` 적용 3곳은 컬러 마스터 투입 완료.

| 자산 | 채도 | 상태 |
|---|---:|---|
| `work-*-sm.webp` / `work-*.mp4` | 11.7 ~ 21.6 | ✅ 컬러 |
| `hero-*.webp` (`-sm` 포함) | 11.5 ~ 14.4 | ✅ 컬러 (원래부터 컬러였음) |
| `about-*.webp` | 0.01 ~ 0.10 | 흑백 — 의도대로 |
| `office*.webp` | 0.01 | 흑백 — 의도대로 |
| `*-poster.webp`, `{seoul,tokyo,taipei}.mp4` | 0.02 | 흑백 — 의도대로 |

포스터 ↔ 영상 1프레임 정합 차이 0.4~1.3 (32×32 그레이 비교). 페이드 이음매 없음.

⚠ CSS 필터는 파일에 없는 색을 되살리지 못한다. **새 자산을 넣을 때는 반드시 컬러 마스터로 넣을 것.**
흑백으로 인코딩된 파일에 `.tone` 을 붙여도 호버 시 아무 일도 일어나지 않는다.

**흑백 유지(컬러화 대상 아님)** — `video/{seoul,tokyo,taipei}.mp4` 와 그 포스터(홈 히어로 3분할), `img/about-*.webp`, `img/office.webp`.

---

### 2.2 ABOUT / COMPANY 분리 (2026-08-17 확정)

**ABOUT = 사상, COMPANY = 실체.** 한 페이지에 선언이 셋(VISION·MISSION·MESSAGE) 있으니
서로 크기를 뺏는 제로섬이 반복됐다. 분리하니 각자 자기 페이지의 주인공이 된다.

```
ABOUT      VISION → MISSION → VALUES → WHY(# × ∞)        2,203px
COMPANY    MESSAGE → PEOPLE → 会社概要                    4,047px
```

#### ABOUT — 나열형

세 선언은 **하나의 흐름**이므로 괘선으로 끊지 않는다. 여백만으로 나눈다.
제목은 크기·굵기·자간을 **완전히 통일**(46.4px / w300 / −0.035em) — 다르면 「부속」처럼 읽힌다.
`.sec` 기본 패딩이 위아래 108px 이라 선언이 연달아 오면 216px 이 쌓인다. `.sec--creed` 로 조인다.

| 폐기한 안 | 사유 |
|---|---|
| 좌우 대비(VISION↔MISSION) | 나열이 선언으로 읽힌다는 대표 판단 |
| MESSAGE 를 ABOUT 최상단 | 페이지 분리로 불필요해짐 |
| VALUES 3단 그리드(01/02/03) | 중앙 정렬에서 칸 사이 여백만 벌어진다 |
| VALUES 화살표 흐름 | 다른 섹션과 형식이 어긋난다 |

**VALUES 는 「多様性」・「共感」・「感動」.** 영문 `Diversity, Empathy, Emotion.` 과 정확히 대응하고
실물 덱 p3 와도 일치한다. 설명은 `違いから、共感へ。共感から、感動へ。` 한 줄.

#### WHY TAG EIGHT — 유일한 반전 화면

**기호는 글자가 아니라 마크라, 본문과 같은 수치면 작아 보인다.**
VALUES 제목(46.4px)의 **약 2배인 90px** 가 균형점(109px 과다 / 74px 부족).
본문은 크기를 본문과 같게 두고 **밝기만 88% 로** 올린다.

#### COMPANY

`MESSAGE`(편지·손글씨) → `PEOPLE`(선택형 5명) → `会社概要`(9행).
**대표 메시지 팝업은 폐기**했다(2026-08-17). 독립 섹션이므로 모달이 불필요하다.

편지 데이터는 `stanzas: { kind?: 'lead'|'close'; lines: string[] }[]`.
**인덱스로 강약을 주면 언어별 연 수가 달라질 때 어긋난다** — 실제로 밟았다(6연 데이터에 9장면 로직).

#### CEO MESSAGE — 분할 지면 (2026-08-17 대표 시안 반영)

**좌 텍스트 44% / 우 풀블리드 인물 56%.** 원고를 8단락으로 줄여 한 화면에 담는다.
스크롤로 읽는 문서가 아니라 **한 장의 지면**으로 다룬다. 데스크톱 `min-height: 88vh`.
1단에서는 **사진이 먼저**(`order:-1`) — 얼굴을 보고 글을 읽는 순서가 자연스럽다.

| 폐기 | 사유 |
|---|---|
| 손글씨체(Klee One 등) | 이 판형에서는 본문 서체가 더 정연하다. **COMPANY 의 `handwriting` prop 제거** |
| 9장면 편지(20행) | 길어서 스크롤 문서가 됐다. 8단락으로 압축 |
| `msg__lead` / `msg__close` 강약 | 단락이 균질해져 불필요 |

**타이포·비율 확정 (2026-08-18)**

| | 전 | 후 |
|---|---|---|
| 헤드라인 | 27.2px | **34.4px** (+26%) |
| 본문 | 13.52px | **16.5px** |
| 본문 굵기 | w300 | **w400** |
| 행간 | 2.3 | 2.15 |
| 문단 간격 | 24px | **18px** |
| 텍스트 : 사진 | 44 : 56 | **50 : 50** |

> **본문 색은 이미 `var(--ink)` 로 최댓값이었다.**
> 「한 단계 진하게」의 실제 지렛대는 색이 아니라 **굵기**(300→400)다.

**서명은 한 줄** — `TAG EIGHT合同会社　代表社員　李東眩`.
마지막 단락 `私たちは、そう信じています。` 만 `font-weight: 500`.

**라벨 아래 28×1px 선 = 대표 개인 색 `#F37021`(Hermès Orange).**
시안의 샴페인 `#B89549` 에서 교체(2026-08-17).

> ⚠ **회사 색이 아니라 개인 색이다.** PEOPLE 의 개인 색 체계와 같은 것이므로
> 새 브랜드 색을 들이는 게 아니고, **이 섹션이 대표 본인의 지면이라서** 성립한다.
> **다른 섹션 라벨(VISION·MISSION 등)로 확산시키지 않는다.** 확산하면 무채 시스템이 무너진다.

**PEOPLE 제목은 폭 제한을 푼다**(`.team__head { max-width: none }`).
`.h-md` 의 `26ch` 는 CJK 에서 어긋난다 — `ch` 는 숫자 `0` 폭 기준이라
실제 필요 폭(390px)과 상한(390px)이 겹쳐 「まず」가 행 끝에 남았다.
**이 문장은 한 줄로 읽혀야 한다.**

**직함은 `代表` 로 통일**(2026-08-17). `代表社員` 은 合同会社 의 등기 용어라
서명에는 무겁다. 会社概要 표의 라벨은 `代表者` 로 별개.

**WHY 섹션 정리**(2026-08-20)

| | 전 | 후 |
|---|---|---|
| 라벨 크기 | 15.2px | **16.8px** |
| 라벨 자간 | **.3em** | **.24em** — VISION/MISSION/VALUES 와 동일 |
| 라벨 밝기 | 62% | 68% |
| `×` 기호 | `w200` 16.8px 45% | **`w400` 30.4px 62%** |
| 라벨 ↔ 기호 | 24px | **65px** |
| 기호 ↔ 본문 | 32px | **70px** |
| 본문 행간 | 1.95 | **2.25** |

> **`.3em` 은 글자가 흩어져 집중이 안 된다.** 다른 섹션 라벨과 같은 `.24em` 으로 맞추고 크기로만 세운다.
> `×` 는 `w200` 이라 큰 기호(`#` `∞`) 사이에서 존재가 사라졌다.

> ❌ **`#` 아래 CONNECT / `∞` 아래 INFINITY 는 추가하지 않는다**(2026-08-20 검토).
> 바로 아래 문장이 `つなぐ「#」と、無限の「∞」。` 로 **같은 말을 이미 하고 있다** — 두 줄 사이 의미 중복.
> 라벨을 넣으면 읽는 순서가 「기호 → 영문 → 일본어」로 한 단계 늘어난다.
> 넣으려면 일본어 문장을 지워야 하는데, 그러면 **이름의 유래 설명을 잃는다.**

**WHY 라벨 확대**(2026-08-19, 위 표로 갱신) — `12.48px → 15.2px`.
이 섹션은 큰 제목 없이 기호(`# × ∞`)와 본문뿐이라 **라벨이 사실상 제목**이다. 일반 라벨과 같은 크기면 약하다.

**WHY 안에 COMPANY 버튼**(2026-08-19). `.why__cta` · `.btn--on-dark`.

```
ABOUT(사상)  →  COMPANY(그 사상을 가진 사람들)
```

> **읽는 순서지 영업이 아니다.** 이름의 유래를 읽은 다음 「그럼 누가 하는가」로 이어진다.
> ⚠ **CAPABILITIES 로 연결하지 않는다** — 사상 페이지가 상품 안내로 바뀐다.
> ⚠ **CTA 섹션(`ClosingCta`)은 붙이지 않는다** — WHY 가 이미 검정이라 검정이 두 번 겹친다.
> 섹션이 아니라 **WHY 안의 버튼 하나**로 해결한다. ABOUT 은 CONTACT 로 닫지 않는 유일한 페이지.

**버튼 문구는 `COMPANY` 가 아니라 `TAG EIGHT`**(2026-08-19).
바로 위가 `その意志を、名前に込めています。` 로 끝나므로 **그 이름을 그대로 눌러 회사로 들어간다** —
이름의 유래를 읽은 직후라 성립하는 문구다.
> ⚠ 목적지가 문구에 없으므로 `aria-label="COMPANY"` 로 보완한다. 스크린리더에서 「TAG EIGHT 링크」만 들리면 어디로 가는지 알 수 없다.
> 여백은 60 → **81px** — 본문이 닫힌 뒤 한 박자 쉬고 이름이 나오게 한다.

#### 대표 사진 (2026-08-17 재생성)

원본 사진을 `nano_banana_2` 로 **얼굴은 유지한 채 씬만 교체**했다.
콘크리트 벽 + 대각 그림자 + 검정 재킷. 「친근하고 크리에이티브한」 방향.

| | |
|---|---|
| 파일 | `/img/ceo.webp` 900×1200 (3:4) · `-sm` 600×800 |
| 톤 | CSS `grayscale(1) contrast(1.1)` |
| 배치 | **제목 옆 340×453.** 편지가 길어 첫 화면이 글자벽이 되는 걸 막는다 |
| 서명 | 사진 제거, 텍스트만 |

**피부 리터치** — 점 42개를 제거했다. **AI 재생성이 아니라 픽셀 인페인팅**이다.
`seedream_v4_5` 로 리터치를 시도했더니 턱선과 머리색까지 바뀌어 다른 사람이 됐다.
OpenCV 블랙햇으로 어두운 점만 검출하고, **눈·눈썹·수염은 좌표로 제외**한 뒤 `INPAINT_TELEA` 적용.
얼굴 형태·표정·피부결은 그대로다. 스크립트: `ceo2/r2.py`

> **AI 표기는 두지 않는다** (2026-08-17 대표 판단).
> 얼굴은 본인이고, 일본에 이런 일반 표시 의무는 없다.
> **ABOUT 의 이미지 3장은 인물 자체가 생성물이므로 표기를 유지한다.**

폐기 — 배경 제거 컷아웃(112px, 서명 옆). 사진이 너무 작아 존재감이 없었다.

#### 내비게이션

```
ABOUT / CAPABILITIES / WORKS / INSIGHT / COMPANY / CONTACT
```

**JOURNAL → INSIGHT 개칭.** 라우트도 `/insight` 로 옮기고 301 리다이렉트 추가.
`/people` 은 `/company#people` 로 갱신.

**총 47페이지** (기존 44 + company 3).

**会社概要 — 法人番号 병기는 제거**(2026-08-18).
`登録番号 T3011103008827` 에서 `T` 를 떼면 그것이 法人番号 다 — **같은 숫자를 두 번 쓰고 있었다.**
인보이스 등록번호는 거래 전 확인 대상이므로 그쪽을 남긴다.

#### 멤버 사진 (2026-08-18)

각자 제공한 사진을 `nano_banana_2` 로 **얼굴은 유지한 채 씬만 통일**했다.
콘크리트 벽 + 대각 그림자 + 소프트 광. 대표 사진과 같은 조건.

**옷·포즈는 일부러 다르게 간다** — 다섯이 같으면 크리에이티브 회사가 아니라 제복 조직으로 보인다.

| | 옷 | 포즈 |
|---|---|---|
| DONGHYUN LEE | 검정 재킷 + 티 | 팔짱 · 정면 |
| YONGJUN PARK | 차콜 니트 + 셔츠 카라 | 주머니 · 우향 3/4 |
| JEEHYUN YONG | 터틀넥 니트 | 손 모음 · 좌향 |
| HESTER LIN | 오버사이즈 셔츠 + 리브 이너 | 한 팔 · 정면 |
| SHISEI AOKI | 오버셔츠 오픈 + 티 | **몸은 옆 · 얼굴은 카메라** |

> **명단 변경(2026-08-18)** — `FILTER J` → **`JEEHYUN YONG`**. 파일 키도 `fj` → `jh`.

**배경 제거 컷아웃**(2026-08-18). 콘크리트 배경을 지우고 **인물이 `#` 마크 위에 뜨게** 한다.
개인 색이 배경 대신 화면을 채우므로 다섯이 나란히 있을 때 색으로 구분된다.

> **⚠ 흑백은 이미지에 굽는다.** CSS `filter: grayscale()` 은 알파와 충돌한다.
> 썸네일도 `object-fit: contain` + 연회색 배경으로 바꿨다 — 투명 컷아웃을 `cover` 로 쓰면 잘린다.

> **⚠ 명도는 차콜~블랙으로 묶는다.** 사이트가 흑백이라 색이 아니라 밝기만 남는다.
> 흰 옷은 밝은 콘크리트 배경에 묻혀 실루엣이 사라진다. 개성은 **소재·실루엣·레이어드**로 낸다.

> **⚠ 얼굴 크기를 맞춘다(19~26%).** 선택형 UI 라 전환할 때 머리 위치·크기가 튀면 인터랙션이 거칠어진다.
> 몸을 옆으로 돌리면 카메라에서 멀어져 얼굴이 14% 까지 작아진다 — 실제로 밟았고 프레이밍을 명시해 재생성했다.
> **완전 옆모습은 쓰지 않는다** — 썸네일이 1:1 이라 얼굴 절반만 남는다.
> 대신 「몸은 옆, 얼굴은 카메라」로 움직임만 살린다.

> 🔴 **본인 확인 필요.** AI 가공한 얼굴이므로 공개 전 각자 OK 를 받는다.
> HESTER LIN 은 원본이 눈을 감고 있어 **눈매를 새로 그렸다**.
> JEEHYUN YONG 은 원본이 얼굴 절반이 잘린 야외 셀피라 **재현도가 가장 낮다** — 정면 사진이 있으면 재생성 권장.

**개인 색 확정**(2026-08-18)

| | 색 | HEX | ink |
|---|---|---|---|
| DONGHYUN LEE | Hermès Orange | `#F37021` | `#B54A15` |
| YONGJUN PARK | Green | `#3F8F63` | `#2F6E4A` |
| JEEHYUN YONG | Blue | `#2E6FB7` | `#245A96` |
| HESTER LIN | Magenta | `#C0417A` | `#9B3060` |
| SHISEI AOKI | Deep Khaki | `#6B7043` | `#4E5230` |

#### 소개문 (2026-08-18~)

**2행 고정.** 이 칸에 그 이상 들어가면 PEOPLE 이 무너지고 멤버 간 위계가 생긴다.
3언어 × 2해상도 전부 2줄로 떨어지는 것을 실측으로 확인한다.

**쓰는 법 — 경력 요약은 쓰지 않는다.**
「어디서 몇 년」은 어느 프로필에도 들어가고 사람이 남지 않는다.
① 자주 듣는 질문 ② 일할 때의 버릇 ③ 판단 기준 — 셋 중 하나를 축으로 잡는다.

| | |
|---|---|
| DONGHYUN LEE | ✅ 확정 |
| YONGJUN PARK | ✅ 확정 |
| JEEHYUN YONG | ✅ 확정 |
| HESTER LIN | ✅ 확정 |
| SHISEI AOKI | ✅ 확정 |

**5명 전원 확정 (2026-08-18). 3언어 × 2해상도 전부 2줄 실측 완료.**

**DONGHYUN LEE 확정문**
```
2004年に来日して、韓国人としてずっと外から日本を見てきました。
価値観の違うふたつの間で、伝わる形を探す。相手が地球規模になっても、飽きません。
```

**농담은 마지막 한 구절에만.** 첫 줄은 「이 사람이 누구인가」를 세우는 자리라
여기서 웃기면 뒤가 다 가벼워진다. 진지하게 오다가 끝에서 한 번 풀면 **여유 있는 사람**이 된다.

> `世界` 를 1행에 넣는 안은 폐기했다 — 2행의 `地球規模` 와 겹쳐 낙차가 죽는다.
> **농담은 처음 나오는 자리에서만 성립한다.**
> `飽きません` 은 부정형이라 담담하다. 「재미있다」고 직접 말하면 가벼워진다.

**YONGJUN PARK 확정문** — 직함 `Content Producer`
```
20年、韓国の読者に向けて日本のことを書いてきました。ブログで、ガイドブックで、いまは企画で。
観光地より、その土地の暮らしのほうが響く。面白いと思ったら、頭より先に手が動きます。
```

**근거는 셋 중 가장 강한 하나만.** NAVER · 20년 블로거 · 가이드북 저자 — 다 넣으면 이력서가 된다.
**20년 블로거**를 축으로 잡았다. 「한국 소비자에게 통하는 걸 안다」의 근거로 가장 직접적이다 —
조사나 감이 아니라 **20년치 독자 반응이 몸에 있다**는 뜻이 된다.

> **서술어가 사람을 정한다** — `知っている` 은 감식안(판단이 정확한 사람),
> `動く` 는 실행력(먼저 손대는 사람). 박용준은 콘텐츠를 **만드는** 사람이므로 `動く`.
> 대표가 `探す`(찾는다)로 끝나므로 **찾는 사람 / 움직이는 사람**으로 역할이 갈린다.
>
> `面白いと思ったら` … 「통한다」는 분석이고 「재미있다」는 취향. **취향으로 움직이는 사람**이 크리에이티브 회사에 맞는다.
> ⚠ `それは、頭より先に手が動きます` 는 문법이 깨진다 — `それは`(=앞 문장)를 손이 「움직인다」로 받을 수 없다.
> 인식은 `知っている` 이 받고, 동작은 조건절(`면 ~하면`)이 필요하다.
>
> `観光地より、その土地の暮らし` … 유명 관광지가 아니라 **생활**을 본다. 이 사람의 판단 기준이자 회사의 차별점.

**JEEHYUN YONG 확정문** — 직함 `Media & Social Producer`
```
韓国の放送局でディレクターをしていました。日本で10年、いまは日本と韓国、その「間」で。
テレビもSNSも動き方が違う。話題がどこをどう通るのかを見てから、方法を考えます。
```

> `その「間」で` … 따옴표가 WHY TAG EIGHT 의 `#`, MISSION 「そのあいだに、立つ」 와 시각적으로 이어진다.
> `ケーブルテレビ` → `放送局` … 일본에서 「ケーブルテレビ」는 지역 방송 이미지가 강해 규모가 작게 읽힌다.
> `どこをどう通るのかを見てから` … 매체마다 다르니 **템플릿을 쓰지 않는다**는 주장.
>
> **서술어는 다섯이 겹치지 않게 한다** — 나란히 놓았을 때 각자 다른 사람이 된다.
> 이동현 `探す`(찾는다) / 박용준 `動く`(움직인다) / 용지현 `考える`(판단한다) / 헤스타 `いる`(거기에 있다).
> 헤스타만 **동작이 아니라 상태**라 오히려 눈에 띈다. 남은 하나는 **정리** 계열이 비어 있다.

**HESTER LIN 확정문** — 직함 `Creative Producer`
```
台湾と日本で13年。いまは台湾・韓国・日本、その三つの「間」で。私自身も、インフルエンサーです。
ブランドが伝えたいことと、フォロワーが知りたいこと。その両方の声が聞こえる場所にいます。
```

> **「본인도 인플루언서」가 축.** 인플루언서 마케팅을 하는 사람이 본인도 팔로워를 갖고 있다 —
> 다른 회사가 흉내낼 수 없는 근거다.
> `両方の声が聞こえる場所にいます` … 「듣는다」가 아니라 **「들리는 곳에 있다」**. 노력이 아니라 위치.
> `声` 가 들어가면서 브랜드와 팔로워가 목소리를 가진 주체가 된다.
> `三つの「間」` … 용지현이 두 나라 사이라면 헤스타는 세 나라 사이. 같은 구조를 반복하며 규모가 커진다.
>
> 🔴 **zh-TW 는 본인 검토 필요** — 본인이 대만인이므로 내가 쓴 것은 초안.

**SHISEI AOKI 확정문** — 직함 `Strategy Producer`
```
ECの現場から、日本の自治体へ。訪日事業とMICEを統括してきました。
人がどこから来て、どこで立ち止まり、何を選ぶのか。企画のスタートは、いつも事実からです。
```

> **혼자 데이터로 말한다.** 앞 넷이 감각·경험·위치로 말하므로, 마지막에 **검증 가능한 것**이 온다.
> 크리에이티브 회사가 감각만 파는 게 아니라는 증거 — 순서상 마지막이라 더 효과적이다.
>
> `どこから来て、どこで立ち止まり、何を選ぶのか` … 유입 → 체류 → 전환.
> **마케팅 퍼널을 사람의 동작으로 말한다.** 전문용어를 쓰지 않고 전문성이 보인다.
> `ECの現場から、日本の自治体へ` … 前職 실명을 빼고 **어떤 데이터를 다뤘는지**만 남겼다.
> EC 와 자치체는 데이터 밀도가 정반대라, 「먼저 사실을 본다」가 태도가 아니라 **몸에 밴 것**이 된다.
> `観光誘致` 는 訪日事業 과 겹쳐 제외. 셋 중 `MICE` 가 가장 구체적이다.

#### 확정 결과 — 다섯이 겹치지 않는다

| | 축 | 서술어 | 직함 |
|---|---|---|---|
| DONGHYUN LEE | 외국인의 위치 | `探す` 찾는다 | Creative Director & CEO |
| YONGJUN PARK | 20년의 감각 | `動く` 움직인다 | Content Producer |
| JEEHYUN YONG | 매체의 움직임 | `考える` 판단한다 | Media & Social Producer |
| HESTER LIN | 양쪽 목소리 | `いる` 거기에 있다 | Creative Producer |
| SHISEI AOKI | 사실 | `事実から` 출발점 | Strategy Producer |

**서술어·직함이 전부 다르다.** 나란히 놓았을 때 각자 다른 사람으로 읽히면서 한 팀이 된다.

**⚠ 2행 유지는 실측으로 확인한다.** 초안이 일본어·한국어에서 3줄로 넘쳤다.
`いまは日本と韓国、その「間」で仕事をしています` → `その「間」で` 로 잘라 2줄에 맞췄다.
**한국어가 가장 먼저 넘친다** — 원고를 쓸 때 한국어 길이를 기준으로 잡는 편이 안전하다.
>
> **프로모션·기획은 나열에 끼워 `いまは企画で` 로만.** 설명하면 CAPABILITIES 와 겹친다.
> 「いまは」가 붙으면 앞의 둘은 과거, 마지막이 현재가 되어 **경력이 아니라 이동**으로 읽힌다.

> **폐기한 표현과 사유** — 재논의 방지
> - `ソウルと東京で15年` … 2004년 도일이므로 **사실 오류**. 연도를 쓰면 매년 고쳐야 하므로 `2004年に来日して` 로 고정
> - `「違う」` … CEO MESSAGE 가 「違う」は、おもしろい。로 시작한다. **같은 페이지 중복**
> - `不便だと切り捨てず` … 좋은 태도지만 대표가 「불편」을 판정하는 구도
> - `理解させる` 계열 … **한쪽이 옳고 한쪽이 배우는 구조**가 된다. 대표 본인이 위화감을 지적
>
> **`伝わる形を探す` 로 착지한 이유** — 가르치지도 설득하지도 않고 **찾는다**.
> 상대에게 아무것도 요구하지 않으면서, MISSION 「そのあいだで、伝わる形をつくる」 를 실무 층에서 받는다.
> 회사가 「만든다」고 선언한 것을 대표가 「찾는다」고 받으므로 반복이 아니라 층이 된다.

---

### 2.2.7 타이포·여백 전 페이지 통일 (2026-08-18)

ABOUT 을 다듬는 과정에서 값을 새로 잡았는데 **나머지 페이지가 옛 값에 남아 있었다.**
실측으로 잡아 `.h-lg` 와 `.page-head` 두 규칙만 고쳐 한 번에 맞췄다.

| | 전 | 후 |
|---|---|---|
| 페이지 제목 | `32.8px w200 −0.03em` | **`46.4px w300 −0.035em`** |
| 상단 패딩 | `144px` (page-head) | **`108px`** (`--section-y` 와 동일) |
| **하단 패딩** | `0` | **`108px`** — 없으면 본문 끝에 다음 섹션 괘선이 바로 붙는다 |
| 본문 행간 | `.lead` 1.9 / `.creed__body` 1.95 | **1.95** |

**ABOUT 기준으로 통일한 이유** — 가장 최근에 정한 값이고 VISION·MISSION·VALUES 세 제목으로 검증했다.
`w200` 은 46px 에서 가늘어 보인다.

**예외 — COMPANY 의 CEO MESSAGE(34.4px w300 +0.05em)** 는 그대로 둔다. 손글씨 성격의 별개 층.

> 라벨(11.52px w400 0.24em)과 본문 색(`--ink-55` = 72%)은 **원래부터 전 페이지 동일**했다.
> 어긋난 것은 제목·패딩·행간 셋뿐이었다.

**CAPABILITIES 도입부 교체**

```
[전] 4행 제목 + 3블록을 미리 요약하는 본문
[후] 文化の違いを、選ばれる理由に変える。
     韓国・日本・台湾。ブランドの想いと、その市場で暮らす人の感覚。
     その「間」に立ち、戦略からクリエイティブ、実行までをつくります。
```

> 구 본문이 「戦略設計からクリエイティブ制作…プロモーション実行まで」로
> **바로 아래 3블록(WE THINK / CREATE / ACTIVATE)과 같은 목록을 두 번** 읽게 했다.
> 도입부는 3블록을 요약할 게 아니라 **왜 이 3단계인가**를 말한다.
>
> `違い → 選ばれる理由` … VALUES `違いから、共感へ` / WHY `違いをつなぎ` 와 같은 키워드를
> 실무 층에서 다른 동사로 받는다. 반복이 아니라 축적.
> `その「間」に立ち` … MISSION 「そのあいだに、立つ。」 를 그대로 받는다.
> **`「間」` 는 이 사이트의 반복 모티프** — 용지현·헤스타 프로필에도 쓰인다.
> `一緒に` 는 상대가 명시되지 않아 제외.

#### 3블록 카피 개정 (2026-08-19)

| | 전 | 후 |
|---|---|---|
| 01 | 戦略設計・現地最適化 | **ブランド戦略・現地最適化** |
| 02 | クリエイティブ制作 | **クリエイティブ開発・コンテンツ制作** |
| 03 | キャンペーン実行・検証 | **プロモーション実行・メディア運用** |
| 항목 | 3개 | **4개** |

**리드 3개가 하나로 이어진다.**
```
見つける → 形にする → つなげる
```
`選ばれる理由` 가 THINK 에서 발견되고 CREATE 에서 형태가 되므로,
CAPABILITIES 제목(`選ばれる理由に変える`)과 본문이 처음으로 맞물린다.

> **홈은 같은 `blocks` 를 쓴다** — `title`/`lead`/`items` 참조. 자동 반영된다(`ja` 는 홈에서 미사용).
> **会社概要 事業内容도 같이 갱신했다** — 3영역 명칭이 어긋나면 회사 정의가 두 개가 된다.
> ⚠ 실물 덱 WHAT WE DO 와 **명칭이 갈렸다.** MISSION·자본금에 이어 세 번째 — 덱 개정 시 일괄 반영 대상.
> **덱은 차후 전체 개정 예정**(2026-08-19 대표 판단). 사이트를 먼저 확정하고 덱을 맞춘다.

**홈 CAPABILITIES 섹션 (2026-08-19)**

```
CAPABILITIES
考える。つくる。届ける。          ← THINK / CREATE / ACTIVATE 와 1:1 대응
ブランド戦略から、コンテンツ制作、プロモーション実行まで。
必要な領域だけでも、すべて一緒でも。
```

| | 홈 | CAPABILITIES |
|---|---|---|
| 일본어 영역명 | **표시**(`.svc-brief__ja` 신설) | 표시 |
| 항목 | **`itemsHome` 3개**(압축) | `items` 4개 |

> 홈 카드가 영문(`WE THINK`)만 보여줘 **무엇을 하는지 알 수 없었다.** 일본어 제목을 세웠다.
> 항목은 홈 3열이라 폭이 좁다 — 4개를 그대로 넣으면 줄바꿈이 생긴다.
> `ブランド戦略・ポジショニング` + `コミュニケーション戦略` → `ブランド・コミュニケーション戦略` 처럼 **병합**해 3개로 줄였다.
> `headHome` 은 세 동사가 3블록과 대응하므로 **블록 순서를 바꾸면 같이 바꿔야 한다**.

**간격 조정 (2026-08-19)**

| | 전 | 후 |
|---|---|---|
| 라벨(`WE THINK`) ↔ 일본어 제목 | **22px** | **8px** |
| 일본어 제목 ↔ 리드 | **0px** | **14px** |
| `考える。` `つくる。` `届ける。` 사이 | 0 | **21px** (`.h-lg--beats`) |

> **위계가 반대로 잡혀 있었다.** 라벨은 제목의 머리이므로 붙이고, 리드와는 띄워야 한다.
> `.svc-brief__meta` 의 `margin-bottom: 1.35rem` 때문에 라벨만 혼자 떠 있었다.
>
> 짧은 문장이 구두점으로 이어지면 **한 덩어리로 읽힌다.**
> `headHome` 을 구두점 단위로 쪼개(`.beat`) 각 동사가 서게 했다.
> 3언어 모두 같은 구조(`생각한다. 만든다. 전한다.` / `思考。創造。傳遞。`)라 자동 적용된다.

#### CAPABILITIES 3블록 정리 (2026-08-18)

| | 전 | 후 |
|---|---|---|
| 영역별 케이스 | **3 / 5 / 2 건** | **2건씩 통일** |
| 케이스 번호 | `01` `02` `07` 표시 | **제거** |
| 시각 요소 | 없음 | **밴드 1장** (도입부와 3블록 사이) |

> **케이스 수가 제각각이면 세 영역의 무게가 달라 보인다.** 더 보려면 WORKS 로 간다 —
> 여기는 「이 영역으로 무엇을 했는가」의 **예시** 자리다.
> **번호는 WORKS 목록의 것이라 여기서는 맥락이 없다.** `01 / 02 / 07` 처럼 건너뛰어 오히려 혼란스러웠다.
> 번호에 의존하던 사진 크롭 분산은 **인덱스 기반**으로 교체했다.

**도입부 순서 (2026-08-19 확정)**

```
사진(밴드)  ←  헤더 바로 아래, 최상단
CAPABILITIES
文化の違いを、選ばれる理由に変える。      ← 1행
韓国・日本・台湾。…その「間」に立ち、
戦略からクリエイティブ、実行までをつくります。
WE THINK …
```

밴드를 도입부 **아래**에 두면 텍스트 → 이미지 → 텍스트로 흐름이 끊긴다.
**최상단으로 올리면** 이미지가 페이지의 문이 되고, 그대로 3블록까지 한 줄기로 내려온다.

| | 값 |
|---|---|
| 밴드 비율 | `21/6` (모바일 `16/9`) — 첫 화면에 제목까지 들어와야 한다 |
| 밴드 여백 | `margin: 0` · 위가 헤더라 여백 불요 |
| 도입부 상단 | `.page-head--band` 로 축소(108 → 72) |
| 제목 | **1행**. 726px 이라 1280px 랩에 들어간다 |
| 본문 | `その「間」に立ち、` 뒤 **명시적 개행** |

> ⚠ **`T` 컴포넌트는 `\n` 을 처리하지 않는다**(BudouX `<wbr>` 삽입 전용).
> 원고의 개행을 살리려면 `body.split('\n')` 후 `<br />` 를 직접 넣어야 한다.

**밴드 — 색 균형의 문제**

```
홈      검정 히어로
본문    ABOUT~CONTACT 전부 흰 면      ← 검정이 없다
푸터    검정
```

가운데가 전부 흰색이라 위아래 검정과 균형이 맞지 않았다.
CAPABILITIES 도입부 아래에 밴드를 하나 두어 **본문에도 무게가 생기게** 한다.

`/img/cap-band.webp` 2200×1015 · `21/8` · 흑백 베이크.
※ 밴드 아래 섹션은 `sec--flush` — **이미지가 이미 구분이므로 괘선까지 두면 두 번**이 된다.
※ `.page-head:has(.capband) { padding-bottom: 0 }` — 밴드가 도입부 안에 있어
**하단 108 + 다음 섹션 상단 108 = 216px** 이 됐다. 밴드로 끝나는 도입부는 아래를 비운다(이미지가 여백 역할).
`:has()` 로 걸어 **밴드가 없는 WORKS·CONTACT·INSIGHT 는 108px 을 그대로 유지**한다.

**3블록 위계 반전 (2026-08-18)**

| | 전 | 후 |
|---|---|---|
| `WE THINK` (영문) | **54.4px w300** | **11.52px w400 · 자간 .24em** (라벨) |
| `戦略設計・現地最適化` (일본어) | 15.68px 72% | **38.4px w300 · 100%** (제목) |

> 읽는 사람이 알아야 하는 건 「WE THINK」가 아니라 **무엇을 하는가**다.
> 접힌 상태에서도 `クリエイティブ制作` `キャンペーン実行・検証` 가 먼저 읽힌다.
>
> ⚠ **영문 3영역명은 문구를 바꾸지 않는다** — 실물 덱 WHAT WE DO 와 락된 명칭이다.
> `STRATEGY` 등으로 바꾸면 덱과 어긋난다. **크기만 낮춘다.**

**블록 내부 정리 (2026-08-19)**

| | 전 | 후 |
|---|---|---|
| 리드문 | 2줄 (`max-width: 34ch`) | **1줄** — `ch` 는 CJK 에서 절반밖에 못 담는다. 폭 제한을 풀고 25.6→20.8px |
| 항목(`市場・生活者分析` 등) | 본문 안, 괘선으로 분리 | **제목 바로 아래**(summary 안) |
| 케이스 배치 | 가로 2열 | **세로 1열** |
| 케이스 구분 | 없음 | 카드 사이 **1px 괘선** |
| 링크 | `WORKS →` 밑줄 텍스트 | **`VIEW MORE →` 버튼**(`.btn`) · 호버 시 검정 반전 |

> ⚠ **카드 전체를 반전시키지 않는다.** 한 번 시도했다가 되돌렸다 —
> 사진이 있는 카드를 통째로 뒤집으면 **시선이 사진으로 가지 않는다**.
> 반전은 홈 WORKS 와 같은 `.btn` 하나가 맡는다. 케이스 안에서는 페이지 하단의 큰 버튼보다 작게(`.capcase__btn`).
> `common.viewMore` 3언어 신설.

> **항목을 제목에 붙인 이유** — 떨어져 있으면 제목이 「이것만 한다」로 읽힌다.
> summary 안에 두면 **접힌 상태에서도 그 영역의 폭이 보인다**.
> **세로 1열** — 가로 2열은 한 건에 주는 시간이 짧아 훑고 지나간다.

> 🔴 **zh-TW 케이스 0건** — `cases.ts` 에 ja 10 / ko 10 / zh-TW 0.
> CAPABILITIES 의 zh-TW 3블록에 케이스가 표시되지 않는다. 번역 대기.
콘크리트 벽에 붙은 시안들 + 대각 그림자 — **인물 사진과 같은 조명 언어**라 사이트 안에서 겉돌지 않는다.
※ 인물 3장(ABOUT)은 이미 썼으므로 재사용하지 않는다.

---

### 2.2.8 클로징 CTA — 컴포넌트화 (2026-08-19)

홈에만 있던 검정 CTA 블록을 `src/components/ClosingCta.astro` 로 추출했다.
**홈도 이 컴포넌트를 쓴다** — 한 곳만 고치면 전 페이지에 반영된다.

| 페이지 | CTA | 사유 |
|---|:--:|---|
| 홈 | ● | |
| CAPABILITIES | ● | |
| WORKS | ● | |
| INSIGHT | ● | |
| COMPANY | ● | **회사 정보를 다 읽은 사람이 가장 문의에 가깝다** |
| ABOUT | — | `.why` 가 이미 **검정으로 닫는다**. 두 번 겹친다 |
| CONTACT | — | 페이지 자체가 창구 |

> **`CONTACT` → `Connect` / `Let's Connect` 안은 폐기**(2026-08-19).
> 같은 블록 안에서 **라벨(CONTACT) ↔ 버튼(CONTACT)** 이 위아래로 맞물리는데 라벨만 바꾸면 대응이 깨진다.
> 내비·페이지 제목까지 네 곳이 어긋난다.
> 이 블록은 이미 `その理由を、一緒につくりませんか。` 로 연결을 말하고 있고,
> 영어 구호를 얹으면 오히려 한 겹 얕아진다.
> `Connect` 는 폐기한 L1 `Asia Business Connected` 의 잔재이기도 하다(폐기 사유: 무엇을 연결하는지 불명확).

**크기 조정 (2026-08-19)** — 컴포넌트를 하위 페이지에 붙이자 **문이 본문보다 커졌다.**

| | 전 | 후 |
|---|---|---|
| `closing__answer` | **52.8px** | **41.6px** |
| `closing__problem` | 25.6px | 20px |
| 패딩 | 5~8.5rem | 4~6rem |
| 블록 높이 | 765px | **641px** |
| 전체 대비 | 홈 15% / 하위 **25%** | 홈 13% / 하위 **22%** |

**위계 반전 (2026-08-19)** — 주역은 **문제**다.

| | 크기 | 색 |
|---|---|---|
| `まだ、選ばれる理由がない。` | **41.6px** | 흰색 |
| `そこから始めるのが、私たちの仕事です。` | **21.6px** · **1행** | **흰색**(회색 아님 — 부속이 아니라 결론) |
| `その理由を、一緒につくりませんか。` | **21.6px** · 1행 | **흰색** — 실제 행동 유도 |
| `直接受託と代理店…対応します。` | **11.52px** · 1행 | 72% — 실무 안내 |

**최종 조정 (2026-08-19)**

| | |
|---|---|
| 문제 ↔ 답 간격 | 26 → **11px** (한 호흡으로 묶는다) |
| 답·초대 색 | `#fff` → **88%** |
| 안내문 | 11.52px 72% → **15.12px 82%** (주석처럼 보여 안 읽혔다) |

> **순백은 주역(문제)만.** 전부 흰색이면 한 번에 다 읽혀 포인트가 죽는다.
> 답·초대를 88% 로 한 단계 내리자 `まだ、選ばれる理由がない。` 가 확실히 앞으로 나온다.

**안내문 카피 교체**
```
[전] 直接受託と代理店・制作会社様経由、どちらの座組にも対応します。
[후] 直接のご依頼にも、代理店・制作会社様を通したご相談にも対応します。
```
`座組` 는 업계 용어라 일반 클라이언트에게 통하지 않는다. `ご依頼`/`ご相談` 이 자연스럽다.

> `sub` 를 `.closing__invite`(초대) / `.closing__note`(안내) **두 층으로 분리**했다.
> 초대문은 답과 같은 크기·흰색 — **여기가 실제 CTA 문장**이라 설명 층에 묻히면 안 된다.
> 실무 안내(座組 대응)만 작게 한 줄.

> **답이 크면 「우리는 이런 일을 합니다」가 되고, 문제가 크면 「지금 이렇지 않나요」가 된다.**
> 클로징은 상대의 현재를 먼저 짚어야 한다.
> `sub` 는 케이스 본문(`.capcase__essence`)과 같은 `var(--step--1)` — 설명 층으로 통일.
> 개행은 원고의 `\n` 을 그대로 쓴다(문장 단위 자동 분할은 위치가 안 맞는다).

> **페이지 제목(46.4px)보다 클로징(52.8px)이 컸다 — 위계 역전.**
> 홈은 히어로가 있어 묻혔지만, 하위 페이지에서는 **클로징이 그 페이지에서 가장 큰 글자**가 됐다.
> 컴포넌트를 여러 페이지에 붙일 때는 **가장 짧은 페이지 기준**으로 크기를 정한다.

---

### 2.2.9 CJK 자간 전면 조정 (2026-08-19)

**`-0.035em` 은 라틴 문자 기준 값이다.** CJK 는 글자 자체가 정사각 프레임 안에 여백을 갖고 있어
음수 자간을 주면 글자끼리 눌린다. 24곳을 성격별로 다시 잡았다.

| 층 | 전 | 후 |
|---|---|---|
| 일본어 큰 제목(`.h-lg` `.creed__head` `.cap__ja` `.closing__problem`) | `-0.035 ~ -0.025em` | **`+0.005em`** |
| 일본어 본문(`.lead` `.creed__body` `.cap__lead` `.closing__*`) | `-0.02em` / 기본값 | **`+0.02em`** |
| 작은 설명(`.svc-brief__ja` `.legal__h`) | `-0.025em` | **`+0.01em`** |
| 영문 라벨(`.label` `VISION` `WHY TAG EIGHT`) | `.24 ~ .3em` | **유지** |

> ⚠ **숫자·라틴 전용은 음수를 유지한다** — `.num`(-0.04) / `.facts__n`(-0.05) / 히어로 영문 캐치(-0.02).
> 라틴은 원래 그렇게 설계된 서체라 벌리면 흩어진다.
>
> ⚠ **`.tm__bio` 만 `+0.008em`** — 본문 표준(0.02)을 주면 **2행 규칙이 깨져 3행으로 넘친다**.
> 실제로 밟았고 낮춰서 되돌렸다. **행 수가 고정된 요소는 자간 변경 후 반드시 실측한다.**

---

### 2.2.11 홈 WORKS — 2×2 카드 · 문자만 (2026-08-19 확정)

```
映画祭のノミネートを、          ← 제목 31.7px, 주역
市場の話題へ広げる。
                          01  ← 번호 112px, 배경 오브제(불투명도 5.5%)
メディア戦略  台湾 → 韓国        ← 분류, 하단
```

**사진 없이 문자만.** 홈이므로 제목이 흥미를 만들고 분류는 뒤에서 맥락을 준다.

| | |
|---|---|
| 배경 | **흰색**. 호버 시 검정 반전 |
| 제목 | `clamp(1.4rem, 2.2vw, 2rem)` — 사진이 없으니 이 크기가 카드를 채운다 |
| 번호 | `7rem` · `rgba(11,11,10,.055)` 우하단. 4장에 리듬을 만든다 |
| 분류 | 하단(`margin: auto 0 0`). **칩·괘선 없음** |
| 호버 | 카드 검정 + 번호 흰 12% + 분류 `scale(1.06)` |

**호버 시 개인색 + 네온**(2026-08-19)

**배경은 흰색 그대로.** 검정 반전은 화면을 뒤집어 무겁다 — 변하는 건 **제목 색 하나**.

**반응은 메타 층이 맡는다**(2026-08-20 수정) — CAPABILITIES 페이지와 같은 원리.

| | 기본 | 호버 |
|---|---|---|
| 제목 (31.7px) | 검정 w300 | **변화 없음** |
| 분류 (`メディア戦略`) | `w300` · 72% | **개인색 + `w500` + `scale(1.12)`** |
| 루트 (`台湾 → 韓国`) | 25% | **`scale(1.08)`** — 색·굵기 그대로 |

| 카드 | 분류 색 | PEOPLE |
|---|---|---|
| 01 | `#F37021` | DONGHYUN |
| 02 | `#3F8F63` | YONGJUN |
| 03 | `#2E6FB7` | JEEHYUN |
| 04 | `#C0417A` | HESTER |

> **제목은 이미 그 화면에서 가장 큰 글자다.** 색까지 얹으면 강조가 겹치고,
> 반응을 작은 층으로 옮기면 **원래 안 읽히던 정보가 살아난다.**
> 분류 기본 굵기를 `w400 → w300` 으로 낮췄다 — **평상시가 가벼워야 호버의 변화가 보인다.**
> 루트는 따라만 간다: 분류가 주역이고 둘 다 색이 바뀌면 어느 쪽을 볼지 모른다.
>
> ⚠ **`.wcard__meta` 의 gap 은 `2rem`(32px)** — `transform: scale()` 은 레이아웃을 밀지 않으므로
> 분류가 1.12배로 커지면 **확대분이 루트를 덮는다.**
> 실측: 가장 긴 `キャンペーン設計・実行`(159px)의 확대분이 **19px**, 기존 gap 16px 로는 3px 겹쳤다.
> 32px 로 올려 전 해상도·2언어에서 최소 13px 여유 확보.
> **확대 배율을 올리면 gap 도 함께 재계산해야 한다.**

> **PEOPLE 개인색 원본을 그대로 쓴다.** 제목은 31.7px 큰 글씨(3:1 기준)라 원본으로 충분하다.
>
> ⚠ **`ink` 변형은 폐기**(`#B54A15` 등). 5.3~7.1:1 로 **원본보다 어두워** 「색이 진해졌다」는 인상을 준다.
>
> ⚠ **PEOPLE 의 `#` 마크가 연해 보이는 건 색이 달라서가 아니라 불투명도 42~46% 때문이다.**
> 실측: `#C0417A` → 화면상 `#E2A8C2` / `#F37021` → `#FABD99`.
> 그 파스텔 값을 텍스트에 쓰면 대비 **1.6~2.0:1** 로 읽히지 않는다.
> **같은 색이라도 불투명도가 걸리면 다른 색으로 보인다** — 화면 색을 눈으로 비교할 때 주의.

**홈 CAPABILITIES 카드에도 같은 색**(2026-08-20) — `.svc-brief__ja` 호버 시
`01 #F37021` / `02 #3F8F63` / `03 #2E6FB7`. WORKS 와 **같은 원본색·같은 전환 곡선**.
3장이므로 01~03 순서를 그대로 쓴다.

**CAPABILITIES 페이지 아코디언**(2026-08-20) — 홈과 반응 대상이 다르다.

| | 호버 시 |
|---|---|
| `WE THINK` (`.cap__en`) | **`scale(1.18)`** — 색·굵기는 그대로 |
| `ブランド戦略・現地最適化` (`.cap__ja`) | **검정 그대로** |
| 항목 (`.cap__items`) | **`scale(1.06)` + 개인색** |

> **사업명은 이미 그 화면에서 가장 큰 글자라 더 강조할 필요가 없다.**
> 반응을 라벨과 항목으로 옮기면 **정보가 적은 쪽이 살아나** 위계가 흐트러지지 않는다.
> 확대는 `transform` — `font-size` 를 쓰면 레이아웃이 밀린다.
> `opacity: 0.6` 으로 전체를 흐리던 방식은 폐기(「비활성」처럼 보였다).

> ✅ **홈과 상세는 반응 대상이 달라도 된다**(2026-08-20 확정).
> ```
> 홈           제목에 색   → 「무슨 일을 하는 회사인가」가 먼저
> CAPABILITIES  항목에 색   → 「그 안에 뭐가 있나」를 확인
> ```
> 홈은 **훑는 자리**, 상세는 **읽는 자리**다. 색 체계는 공유하되 비추는 대상이 다른 게 맞다.
> 어디서나 똑같이 반응하면 페이지의 목적 차이가 사라진다. **통일하지 말 것.**

**개인색이 쓰이는 곳** — PEOPLE(`#` 마크, 불투명도 42~46%) / 홈 WORKS 4장 / 홈 CAPABILITIES 3장 / CAPABILITIES 페이지 3영역.
순서는 어디서나 `01 오렌지 · 02 그린 · 03 블루 · 04 핑크` 로 고정.

> ⚠ **네온(`text-shadow`)은 쓰지 않는다** — 무채색 사이트에서 깔끔하지 못하다.
> 흰 배경으로 전환하며 이미 제거됐고, 두 섹션 모두 `text-shadow: none` 을 실측 확인했다.
> (히어로의 `text-shadow` 3곳은 사진 위 텍스트 가독성용이라 별개다.)

**전환은 `.22s cubic-bezier(.22,1,.36,1)`** — `ease .4s` 는 시작이 굼떠 「딱딱 끊긴다」고 느껴진다.
빠르게 들어오고 천천히 잦아드는 곡선으로 바꾸고 시간을 절반으로 줄였다.

**호버 시 굵기도 한 단계**(2026-08-19) — `w300 → w400`.
색 변화와 겹쳐 「누를 수 있다」가 분명해진다.
> ⚠ **굵기 변경은 글자 폭을 바꿔 줄바꿈이 튈 수 있다.** ja·ko × 4해상도로 기본/호버 줄 수를 실측해
> 변동이 없음을 확인했다. `w500` 은 무채색 톤에서 무겁다.

>
> ⚠ **명도를 올린 안은 폐기**(`#429668` `#4788D1` `#CB6392`).
> 검정 배경 대비를 맞추려 밝기만 올렸더니 **채도가 그대로라 색이 쨍해졌다**
> (블루 L 44.9→54.9 / 핑크 L 50.4→59.2, 채도 불변).
> PEOPLE 은 흰 배경이라 같은 색이 차분해 보인다 — **배경이 바뀌면 같은 값도 다르게 읽힌다.**
> 어두운 블루·핑크는 **네온 글로우가 보완**하므로 원본으로 충분하다.
> 글로우 불투명도만 `55→66`, `22→26` 으로 살짝 올렸다.
>
> 네온은 `text-shadow: 0 0 18px {c}66, 0 0 42px {c}26` — **획 바깥으로만** 퍼뜨린다.
> 안쪽까지 넣으면(강한 네온안) `w300` 얇은 획이 흐려져 오히려 안 읽힌다.
> 배경 번호도 같은 색을 13% 로 머금어 카드 전체가 그 색을 띤다.
>
> **SHISEI 카키(`#6B7043`)는 홈에 등장하지 않는다** — 홈 케이스가 4장이라.
> WORKS 페이지(10건)로 확장할 때 5색 순환하면 전원이 나온다.

> **폐기 — 세로 1열 리스트**(사진 포함). 크리에이티브하지만 홈에는 무겁고,
> 케이스 이미지가 없어 4행이 같은 도시 사진 반복으로 보였다.
> **폐기 — 회색 칩**(`#f2f2f1`). 무채색 화면에 배경색이 하나 더 늘면 탁해진다.

> 🔴 **사고 기록** — 세로 리스트 스타일을 제거할 때 **범위를 넘겨 CSS 13,000자를 잘라냈다**
> (`.vpanel` 히어로 / `.svc-brief` CAPABILITIES 카드 / `.sector` CLIENTS 등 27개 셀렉터 소실).
> 모바일 가로 넘침 150px 로 발견 → v111 zip 에서 복원 후 카드 블록만 **행 번호 범위**로 교체.
> **교훈: CSS 블록 삭제는 시작·끝 마커를 모두 검증하고, 삭제 길이를 assert 로 상한 검사한다.**

---

### 2.2.11-old 세로 1열 리스트 (폐기)

```
01  [사진]  メディア戦略  台湾 → 韓国
            映画祭のノミネートを、市場の話題へ広げる。
```

가로 2열 카드는 설명을 지운 뒤 **비어 보였다.** 정보를 뺐는데 그 자리를 채우지 않은 탓.
세로 1열 + 사진으로 바꿔 **사진이 무게를 만들고, 분류와 제목이 위아래로 붙어 한 덩어리**로 읽힌다.

| | |
|---|---|
| 그리드 | `3.25rem 15rem 1fr` — 번호 · 사진 · 본문 |
| 사진 | `3/2`, `destOf(route)` 로 목적지 도시 이미지. `object-position` 을 행마다 어긋내 같은 사진이 반복돼 보이지 않게 |
| 분류 | **칩·괘선 없음.** 세로 리스트는 왼쪽 정렬이 이미 정리감을 준다 |
| 호버 | 사진 `scale(1.05)` + 분류 `scale(1.06)` 및 색 진해짐 |

> **폐기한 안** — 메타를 오른쪽 끝에 두는 배치(A·B). 제목을 읽고 다시 오른쪽으로 시선이 왕복해
> 4건을 훑을 때 피로하다.
> **회색 칩도 폐기** — 세로 정렬에서는 불필요하고, 배경색이 하나 더 늘면 무채색 화면이 탁해진다.

> 🔴 **사진은 도시 히어로 이미지 재사용** — `cases.ts` 에 `image` 필드가 없다.
> 케이스별 이미지가 준비되면 필드를 추가하고 교체한다. 지금은 4행이 비슷해 보이는 게 이 때문.

---

### 2.2.10 홈 WORKS 카드 — 설명 제거 (2026-08-19)

```
[전]  메타 · 제목 · 설명 2줄(11.52px)     그리드 483px
[후]  메타 · 제목(23px 1행)               그리드 413px
```

**홈은 목록이지 본문이 아니다.** 4건을 훑는 자리에서 2줄 설명은 아무도 읽지 않는다.
설명(`essence`)은 상세 페이지가 맡고, 홈은 **제목을 실제로 읽히는 크기**로 세운다.

> 제목이 커지면 카테고리·루트가 상대적으로 물러나 **위계가 잡힌다.**
> 전에는 메타·제목·설명이 비슷한 무게라 눈이 어디 둘지 몰랐다.

**⚠ 한 줄 상한은 한국어 기준으로 잡는다.**
실측: 1440px 에서 일본어는 26.2px 까지 1행, **한국어는 같은 문장이 더 길어 먼저 2행으로 넘친다.**
25px 로 잡았다가 ko 에서 깨져 **`clamp(1.2rem, 1.6vw, 1.45rem)`(상한 23.2px)** 로 하향.

| 폭 | 폰트 | ja / ko |
|---|---|---|
| 1440px | 23.04px | 전부 1행 |
| 1280px | 20.48px | 전부 1행 |
| 1100px | 19.2px | 전부 1행 |
| 390px | 19.2px | 2행 (폭상 불가피) |

> 폐기한 안 — `A` 설명 확대(카드에 3층이 그대로라 빽빽), `B` 제목 25.6px(C의 축소판),
> `D` 설명 1줄 클램프(문장이 중간에 잘려 어중간).

**메타도 함께 키운다** — 설명을 지운 뒤 **이곳이 유일한 정보 층**이 됐다.

| | 전 | 후 |
|---|---|---|
| `.wcard__cat` / `.wcard__route` | 11.52px | **19.2px** (모바일 15px) |
| 자간 | .14em / .10em | .06em / .04em |
| 메타↔제목 간격 | 1.1rem | **1.6rem** |
| `min-height` | 15rem | **12rem** — 설명이 빠져 제목 아래 48px 이 비었다 |

> 11.52px 은 제목(23px)의 절반이라 「보이긴 하는데 안 읽히는」 상태였다.
> **메타를 제목과 같은 23px 로 올리는 안은 폐기** — `キャンペーン設計・実行 — 台湾 → 韓国` 이
> 한 줄에 안 들어가 접히고, **카테고리명 길이가 케이스마다 달라 카드 높이가 어긋난다.**
> 19.2px 이 1줄을 유지하는 상한.

**위계 재조정**(2026-08-19) — `w500` + 100% 검정은 **크기가 작아도 시선을 끈다.**
제목(23px w300)보다 카테고리(19.2px **w500**)가 세게 보여 어색했다.

```
카테고리   w500 · 100%  →  w400 · 72%     강조는 호버가 맡는다
```

**루트는 연회색 칩**(`#f2f2f1`). `—` 구분선은 칩이 대신하므로 제거.

> ⚠ **검정 채움 칩은 폐기** — 카드에서 가장 어두운 덩어리가 되어 **루트가 제목보다 먼저 읽힌다.**
> 방금 고친 문제(분류가 주역이 되는 것)를 되살린다. 호버 시 카드가 검정이 되면 칩이 사라지는 문제도 있다.
> 테두리 안은 카드 내 유일한 테두리라 이질적이었다.

**칩 위치 정렬** — `.wcard__cat { min-width: 11.5em }`
> 카테고리 길이가 제각각(`メディア戦略` / `キャンペーン設計・実行`)이라
> 그냥 두면 칩 위치가 카드마다 흔들린다. 최소 폭을 줘서 **4장이 같은 X 좌표**에서 시작한다(간격 18px 균일).
> 모바일 1열에서는 `min-width: 0` 으로 해제.

**호버 시 카테고리 확대**(2026-08-19)
```css
.wcard:hover .wcard__cat { transform: scale(1.09); }
```
> `font-size` 가 아니라 `transform` 을 쓴다 — **레이아웃을 밀지 않아 카드 높이가 흔들리지 않는다.**
> 실측: 121 → 132px, 카드 높이 192px 불변. `prefers-reduced-motion` 시 색만 전환.

> 🟡 **23.2px 은 임시값이다**(2026-08-19). ko / zh-TW 케이스 원고를 새로 쓸 예정 —
> 확정 후 **다시 실측해서 상향 가능**하다. 일본어만 보면 26.2px 까지 여유가 있다.

---

### 2.3 히어로

```
TAG EIGHT LLC.                            ← 누구인가
ASIA BUSINESS CONNECTED CREATIVE AGENCY   ← 무엇을 하는가  (캐치)
韓国・日本・台湾の3市場をつなぐ              ← 어디서 하는가
──────────────────────────
心が動く、その瞬間。                        ← 왜 우리인가
```

- 배경: 서울 → 도쿄 → 타이베이 정지 이미지 3장이 스크롤에 따라 교차
- 인물은 **얼굴을 프레임 밖으로 자른 파편**(손·폰·턱선). 도시가 주역
- `<h1>` = 정체(회사명 + 캐치 + 시장). 브랜드 스테이트먼트는 `<p>`

**핵심 판단**: `h1`에 시(詩)를 넣으면 기계가 회사 정체를 못 읽는다.

### 2.4 콘텐츠 게이트

케이스 10건은 **2층으로 나눈다.**

| | 사이트 공개 | 자료청구 PDF |
|---|---|---|
| 카테고리·방향 | ○ | ○ |
| 課題 / 解決策 / 本質 | ○ | ○ |
| 클라이언트 실명 | ✕ | ○ (동의 시) |
| 실적 수치 | ✕ | ○ |
| 캠페인 비주얼 | ✕ | ○ |

매체명(NAVER·Instagram·YouTube)도 공개 레이어에서 제외한다.
「그 나라의 검색 행동에서 역산」처럼 **사상은 남기고 방법은 감춘다.**

---

## 3. 현재 구현 상태

### 3.1 실측치 (2026-08-06 빌드)

| 항목 | 값 |
|---|---|
| 총 페이지 | **41** (ja 17 / ko 17 / zh-TW 7) |
| 원본 HTML 평균 본문 | **695자** (이전 사이트 0자) |
| 이미지 자산 | 26개 / 830KB |
| dist 전체 | 2,003KB |
| `<wbr>` 삽입 | 1,445개 (BudouX 문절 경계) |
| JSON-LD | 전 페이지 O |
| hreflang | 전 페이지 O |
| OG 이미지 | O (`/og.jpg`, 1200×630) |
| 가로 스크롤 넘침 | 데스크톱·태블릿·모바일 전부 **0** |

### 3.2 페이지별

| 페이지 | 상태 | 비고 |
|---|---|---|
| HOME | ✅ | 히어로 → 스테이트먼트 → WORKS → CLIENTS → SERVICE → 클로징 |
| ABOUT | ✅ | 인물 타일 3장 + MISSION/VALUES + 会社概要 |
| SERVICE | ✅ | THINK / CREATE / ACTIVATE, 03만 검정 반전 |
| WORKS 목록 | ✅ | 사진 없이 타이포만 |
| WORKS 상세 ×10 | ✅ | 도착 시장의 거리 사진이 상단 배너 (`destOf()` 자동 판별) |
| PEOPLE | 🟡 | 명단만. **실제 멤버 사진 없음** |
| CONTACT | 🟡 | 오피스 사진·폼 UI 있음. **전송처 미설정** |
| JOURNAL | 🔴 | 빈 페이지 |

### 3.3 소스 구조

```
src/
├─ data/
│  ├─ company.ts   회사 정보 단일 소스. 会社概要 표시 + JSON-LD 가 참조
│  ├─ i18n.ts      3개 언어 카피 사전. 문장 수정은 전부 여기
│  └─ cases.ts     케이스 10건 (ja/ko). 목록·상세·이전다음이 자동 생성
├─ sections/       페이지 본문 (lang 을 받아 렌더)
├─ components/     Head(SEO) / Header / Footer / Logo
└─ pages/
   ├─ *.astro        일본어 (루트)
   └─ [lang]/*.astro 한국어·번체중문 (같은 섹션 재사용)
```

**문구 수정은 `i18n.ts` 한 파일.** 페이지 파일을 건드릴 일은 거의 없다.

---

## 3.4 브랜드 문서 대조 (2026-08-13)

`claude_TAG8_브랜드메시지_확정판_v1.md` / `claude_TAG8_비전_기준문서_v1.md` 와 대조한 결과.

| 항목 | 상태 |
|---|---|
| L1 `Asia Business Connected Creative Agency` | ✅ 히어로 h1 |
| L3 태그라인 (国境を越えて〜) | ✅ 히어로 밴드 |
| VISION / MISSION / VALUES | ✅ 실물 문서와 문자 단위 일치 |
| WE THINK / CREATE / ACTIVATE | ✅ 실물 확정 명칭 유지 (개명 제안 반려) |
| **Cross-Border 계열 폐기** | ✅ **2026-08-13 제거 완료** (3개 언어 title·description·事業内容) |
| **L2 `選ばれる理由を設計する。`** | ❌ **미배치.** 문서상 홈 필수 |
| L1-J 표기 | 🟡 문서 `〜つなぐクリエイティブエージェンシー` / 사이트 `〜3市場をつなぐ` |
| 케이스 번호 오류 | 🟡 기준문서는 07 중복, PRD는 08/09 크로스 — **PDF 원본 확인 필요** |

---

## 3.5 빌드 품질 수정 (2026-08-14)

전 페이지 자동 검증(41p × 3뷰포트)에서 나온 결함을 처리했다.
검증 도구: headless Chromium — 가로넘침 / JS에러 / alt누락 / 푸터 위치 / 내부링크·hreflang 200 확인.

| # | 결함 | 원인 | 조치 |
|---|---|---|---|
| F1 | 짧은 페이지에서 푸터 아래 흰 여백 (최대 209px) | `body`에 `min-height` 없음 | `body{min-height:100dvh;display:flex;column}` + `main{flex:1 0 auto}` |
| F2 | **언어 전환 링크 10건이 404** | 푸터 스위처가 zh-tw 케이스 상세를 무조건 링크 | `langsFor(path)` 로 존재하는 언어만 렌더 |
| F3 | **hreflang 10건이 404를 가리킴** | `Head.astro`가 LANGS 전부를 alternate로 출력 | 동일 헬퍼 적용. x-default도 조건부 |
| F4 | `-sm` 이미지가 존재하나 미사용 → 모바일이 풀사이즈 수신 | `srcset` 미연결 | ABOUT 타일 / CONTACT 오피스 / 케이스 히어로에 `srcset`+`sizes`+`width/height` |
| F5 | 미사용 자산 18개 (508KB) 배포 | 이전 이터레이션 잔재 (`tex-*` `svc-*` `hero-v-*` `work-*.webp`) | 삭제 |
| F6 | 미디어 톤 규칙 부재 — 위치마다 제각각 | 공통 규칙 없음 | `.tone` 단일 클래스로 통일. §2.2.1 |
| F7 | **`srcset` 폭 서술자·`width/height` 가 실제 파일과 불일치** | 컬러 자산 교체로 해상도가 바뀌었는데 코드에 옛 치수가 하드코딩돼 있었음 | 실측값으로 정정 (아래 표) |
| F8 | **본문 1행이 22자에서 접힘** (기준 32~42자) | `--measure: 44ch` — `ch` 는 숫자 `0` 폭 기준이라 CJK 에서 1글자 ≈ 2ch | `--measure-cjk: 40em` 신설, `.lead` 에 적용 → **40자** |

**F8 상세** — ABOUT 이 「허전」했던 진짜 원인이다. 장식이 부족한 게 아니라
본문이 넓은 여백 한가운데서 22자짜리 좁은 리본으로 접혀 있었다.
`.lead` 를 쓰는 5개 페이지(ABOUT·CAPABILITIES·WORKS·PEOPLE·CONTACT·JOURNAL)가 같이 정상화됐다.

> **규칙** — CJK 본문의 폭 제한에 `ch` 를 쓰지 않는다. `1em ≈ 1글자` 인 `--measure-cjk` 를 쓴다.
> 라틴 전용 텍스트에만 `--measure`(44ch) 를 남긴다.

**F2·F3이 이 프로젝트에서 가장 무거운 결함이었다.** 「검색·AI에 읽히는 상태」가 목적인데
hreflang이 없는 페이지를 가리키면 Search Console에 색인 오류로 잡힌다.

새 규칙 — **경로별 언어 가용성은 `cases.ts:langsFor()` 단일 소스로 판정한다.**
zh-tw 케이스 번역이 들어오면 이 함수가 자동으로 3개 언어를 돌려주므로 추가 수정이 필요 없다.

### 수정 후 실측

| 항목 | 전 | 후 |
|---|---|---|
| dist | 2.9MB | **2.5MB** |
| 내부 링크 404 | 10건 | **0건** |
| hreflang 404 | 10건 | **0건** |
| 푸터 아래 빈 공간 | 최대 209px | **0px** (41p × 3뷰포트) |
| 모바일 `/about` 이미지 | 132KB | **58KB** |
| 모바일 `/contact` 이미지 | 108KB | **53KB** |
| 모바일 케이스 상세 이미지 | 32KB | **24KB** |

미해결로 남긴 것 — `hero-tokyo*.webp`(80KB)는 현재 어느 케이스도 도착지가 일본이 아니라 미사용이지만,
`destOf()` 맵이 참조하는 자산이므로 삭제하지 않는다. 일본 도착 케이스가 추가되면 즉시 필요해진다.

---

## 4. 남은 작업

### 4.1 공개 전 필수 (블로커)

| # | 항목 | 필요한 것 | 담당 |
|---|---|---|---|
| B1 | **PEOPLE 멤버 사진 5장** | 실제 팀 사진 원본 | 대표 |
| B2 | **문의 폼 전송처** | Formspree 가입 → URL | 대표 |
| B3 | **프라이버시 폴리시 페이지** | 원문 확정 | 대표 + 작성 |
| B4 | **GitHub push + Cloudflare 연결** | 계정 작업 | 대표 |
| B5 | **PDF 케이스 08/09 크로스 수정** | 원본 PPTX 수정 | 대표 |

**B1은 AI로 대체 불가.** 실제 멤버 페이지에 가상 인물이 들어가면 회사 신뢰가 무너진다.

**B3**: 일본 개인정보보호법상 폼 운영 시 취득 목적 고지가 필요하다. 폼이 살아나기 전에 처리.

### 4.2 공개 후 (논블로커)

- 번체중문 케이스 10건 번역 (현재 ja/ko만, zh-TW는 준비중 화면)
- JOURNAL 아티클 (월 1편 발행 가능 여부에 따라 존폐 결정)
- Keystatic 관리화면 연결 (저널 갱신 부담 해소)
- 히어로 브랜드 필름 (현재 정지 3장으로 대체 중)
- 건물 외관 사진 (ACCESS 블록용, 직접 촬영 필요)

---

## 5. 사이트 공개 수치 — 팩트체크 상태

사이트에 **외부로 나가는 숫자**만 대상. 빌드 메트릭(페이지 수·용량 등)은 자체 측정이라 제외.

### PASS — 검증 통과 (4건)

| 위치 | 수치 | 근거 | 기준 |
|---|---|---|---|
| HOME 스테이트먼트 | 3 시장 | 사업 사실 | — |
| HOME 스테이트먼트 | 2019 설립 | 등기 (2019년 5월) | 법인등기 |
| HOME 스테이트먼트 | 5 멤버 | 자사 인원 | 2026년 7월 현재 |
| HOME 스테이트먼트 | 6 영역 | Company Profile CLIENTS 6분류 | 자사 자료 |

### CUT — 사이트에서 제외 (전량)

케이스 10건의 성과 수치는 **공개 레이어에 하나도 올리지 않았다.** 게이트 설계상 의도된 것이며,
동시에 아래 항목들이 미검증 상태이므로 결과적으로 리스크를 회피했다.

### ⚠ 미해결 — PDF에 남아 있는 수치

**자료청구 PDF로 나가면 그대로 노출된다. 송부 전 처리 필수.**

| 케이스 | 수치 | 문제 | 조치 |
|---|---|---|---|
| 09 | 총 접촉자수 약 **7,800만명** | 한국 인구(약 5,100만)를 초과. 중복 포함 도달수로 추정 | 「延べリーチ」로 명기하거나 CUT |
| 05 | 검색률 **300% 증가** | 지표 정의 불명, 기준 시점 불명 | 지표명·기준월·출처 명기 or CUT |
| 06 | 돈키호테 매출 상위 브랜드로 정착 | 근거 불명 | 클라이언트 제공 자료 확인, 없으면 CUT |
| 03 | 코멘트 참가율 56% / 네거티브 0.9% | 분모 없음 | `N=◯◯件` 추가 |

### 대표 확인 필요

1. **PDF 케이스 08/09의 成果·本質이 서로 뒤바뀌어 있다.** 웹 코드는 교정 완료, PDF 원본은 미수정
2. 위 4건 수치의 원자료 유무
3. 케이스 실명 게재 동의 가능한 클라이언트 범위

---

## 6. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| **DNS 전환 시 MX 레코드 오조작** | `info@tag-8.com` 전면 중단 | A 레코드만 변경. MX 5개·SPF TXT 절대 미변경 |
| 케이스 실명 무단 게재 | 거래 관계 손상 | 공개 레이어에서 전량 제외 (조치 완료) |
| 미검증 수치의 PDF 노출 | 제안서 전체 신뢰 손실 | §5 처리 후 송부 |
| 로고 워드마크 오타 | 명함·인쇄물까지 확산 | 웹용 SVG 교정 완료. **원본 .ai 미수정** |
| STUDIO 조기 해지 | 롤백 불가 | 신규 사이트 안정화 확인 후 해지 |
| 저널 방치 | 「관리 안 하는 회사」 신호 | 월 1편 불가 시 메뉴에서 제외 |

### DNS 현황 (2026-08-05 조회)

```
MX   aspmx.l.google.com 외 4개   ← Google Workspace. 건드리지 않는다
TXT  v=spf1 include:_spf.google.com   ← 건드리지 않는다
TXT  google-site-verification=...     ← 건드리지 않는다
A    35.194.122.208                   ← 이것만 교체
NS   dns1~5.name-services.com
```

---

## 7. 성공 지표

| 지표 | 정의 | 이전 | 목표 |
|---|---|---|---|
| 원본 HTML 본문량 | 페이지 평균 | **0자** | 500자+ (현재 695자 ✅) |
| 색인 페이지 수 | GSC 기준 | 0 | 41 |
| 언어 | — | 1 | **3** (현재 3 ✅, zh-TW 케이스 미완) |
| AI 인용 | ChatGPT·Perplexity·네이버 AI탭에 「TAG EIGHT」 질의 시 자사 인용 | 미측정 | 3개월 내 발생 |
| 문의 CVR | 세션 대비 폼 제출 | 미측정 | 베이스라인 확보 후 설정 |
| 월 호스팅 비용 | — | STUDIO 구독 | **0엔** |

**AI 인용은 월 1회 수동 체크.** 자동 측정 수단이 없다.

---

## 8. 다음 액션 3개

1. **GitHub 저장소 생성 + push → Cloudflare Pages 연결** (`DEPLOY.md` 참조)
2. **`.pages.dev` 임시 주소에서 전 페이지 검수** — 도메인은 그 다음
3. **팀 5명 사진 + Formspree URL 확보** — 이 둘이 공개의 마지막 관문

---

## 부록 — 되돌린 결정 기록

같은 논의를 반복하지 않기 위해 남긴다.

| 시도 | 왜 폐기했나 |
|---|---|
| 명조(Shippori Mincho) + 극단적 여백 | 사진이 들어오면서 산세리프가 맞았다 |
| 얇은 선 개념도 | 상징이 아니라 「설명도」였다 |
| 석고 ∞ 오브제 | 회사가 아니라 미술관이 됐다 |
| 인물 초상 3분할 | 얼굴이 주인공이 되어 「캠페인 페이지」로 읽혔다 |
| 오렌지 글래스모피즘 | 코퍼레이트 컬러 위반 + 2021년 SaaS 템플릿 언어 |
| SNS 아이콘·세계지도 | 수단이지 정체가 아니다 |
| 12초 브랜드 필름 (108크레딧) | 히어로 배경으로 존재감 애매. 정지 3장이 나았다 |

**공통 원인**: 「무엇을 말해야 하는가」보다 「어떤 느낌인가」를 먼저 정했다.
